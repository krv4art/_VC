# 🚀 Быстрый старт - Google Play Console Uploader

## За 5 минут до первой загрузки

### Шаг 1: Запустите стартовый скрипт
```bash
START.bat
```

Откроется меню с опциями.

### Шаг 2: Выберите опцию 3 (Экспортировать данные)
```
Введите номер (1-7): 3
```

Система создаст три файла:
- `upload-data.json` - для просмотра
- `upload-data.csv` - для Excel
- `browser-upload-script.js` - **для загрузки в Google Play**

### Шаг 3: Откройте Google Play Console

1. Перейдите на https://play.google.com/console
2. Выберите ваше приложение
3. Нажмите **Ваше приложение** → **App Store listing** (или Listing)

### Шаг 4: Откройте консоль браузера

1. Нажмите **F12** (или **Right-click → Inspect → Console**)
2. Откройте файл `browser-upload-script.js` в текстовом редакторе
3. **Скопируйте всё содержимое**
4. **Вставьте в консоль браузера** (в черное окно в нижней части)
5. Нажмите **Enter**

### Шаг 5: Загрузите языки

В консоли браузера введите команду для каждого языка:

```javascript
// Для английского:
await uploadLanguage('en');

// Для русского:
await uploadLanguage('ru');

// Для греческого:
await uploadLanguage('el');
```

После каждой команды ждите, пока закончится загрузка (видно в консоли).

### Шаг 6: Сохраните

1. Найдите кнопку **Save** в Google Play Console
2. Нажмите её
3. Дождитесь сохранения

## 💡 Полезные команды

### Посмотреть статистику
```bash
START.bat
# Выберите 1
```

Покажет количество языков, полей и символов.

### Посмотреть предпросмотр одного языка
```bash
START.bat
# Выберите 2
# Введите код языка (например: ru)
```

### Посмотреть отдельный язык в CSV (Excel)
```bash
START.bat
# Выберите 3
# Откройте upload-data.csv в Excel
```

## ⚠️ Важно

### Ключевые слова и промо-текст слишком длинные!

Скрипт предупредит вас о том, что некоторые поля превышают лимит Google Play:

```
⚠️ Внимание: keywords длиннее чем 100 символов (264)
```

**Что это значит?**
- Google Play Console обрежет текст автоматически
- Или вы можете отредактировать файлы перед загрузкой
- Отредактируйте файлы в `acs\store_listings\shared\[lang]\`

**Решение:**

1. Откройте `acs\store_listings\shared\ru\keywords.txt` в редакторе
2. Уменьшите текст до 100 символов
3. Сохраните файл
4. Запустите экспорт заново

## 📱 Какие языки загружать?

У вас есть 31 язык:

| Основные | Дополнительные |
|----------|---|
| en (English) | ar (العربية) |
| ru (Русский) | cs (Čeština) |
| el (Ελληνικά) | da (Dansk) |
| ja (日本語) | de (Deutsch) |
| zh-CN (简体中文) | fi (Suomi) |
| zh-TW (繁體中文) | fr (Français) |
| es (Español) | hi (हिन्दी) |
| pt-BR (Português) | hu (Magyar) |
| | id (Indonesia) |
| | it (Italiano) |
| | ko (한국어) |
| | nl (Nederlands) |
| | no (Norsk) |
| | pl (Polski) |
| | pt-PT (Português) |
| | ro (Română) |
| | sv (Svenska) |
| | th (ไทย) |
| | tr (Türkçe) |
| | uk (Українська) |
| | vi (Tiếng Việt) |

**Совет:** Начните с основных языков (en, ru, es, de, fr), потом добавьте остальные.

## 🔍 Проверка перед загрузкой

Перед загрузкой всегда проверяйте:

1. **Статистику:**
   ```bash
   START.bat
   # Выберите 1
   ```

2. **Предпросмотр:**
   ```bash
   START.bat
   # Выберите 2
   # Введите язык
   ```

3. **CSV в Excel:**
   ```bash
   START.bat
   # Выберите 3
   # Откройте upload-data.csv
   ```

## ❌ Если что-то не работает

### Консоль браузера показывает ошибку
1. Убедитесь, что вы вошли в Google Play Console
2. Убедитесь, что язык существует в консоли (может быть не активирован)
3. Проверьте селекторы в скрипте (может измениться интерфейс Google)

### Скрипт не находит поля
Google Play Console может измениться. Попробуйте:
1. Обновите страницу (F5)
2. Очистите кэш браузера (Ctrl+Shift+Delete)
3. Используйте другой браузер

### Изменения не сохраняются
1. Найдите кнопку **Save** в Google Play Console
2. Убедитесь, что у вас есть права на редактирование
3. Попробуйте сохранить одно поле вручную

## 📝 Основные файлы

```
e:\_VC\
├── START.bat                    ← Главное меню (запустите это!)
├── QUICK_START.md              ← Этот файл
├── UPLOAD_GUIDE.md             ← Полная документация
├── upload-manager.js           ← Менеджер данных
├── upload-data.json            ← Экспортированные данные (JSON)
├── upload-data.csv             ← Экспортированные данные (CSV)
├── browser-upload-script.js    ← Скрипт для Google Play Console
├── browser-console-uploader.js ← Альтернативный способ
├── google-play-uploader.js     ← Автоматизация через Puppeteer (продвинутый)
└── acs\store_listings\shared\
    ├── en\   (English)
    ├── ru\   (Русский)
    ├── el\   (Ελληνικά)
    └── [другие языки...]
```

## 🎯 Рабочий процесс

```
1. Запустить START.bat
2. Выбрать опцию 3 (Экспорт)
3. Открыть browser-upload-script.js
4. Скопировать в консоль Google Play Console
5. Выполнить: await uploadLanguage('en');
6. Выполнить: await uploadLanguage('ru');
7. И т.д. для остальных языков
8. Нажать Save в Google Play Console
```

## 📞 Нужна помощь?

1. Проверьте [UPLOAD_GUIDE.md](UPLOAD_GUIDE.md) - полная документация
2. Посмотрите видео о Google Play Console
3. Проверьте официальную справку Google Play: https://support.google.com/googleplay

---

**Версия:** 1.0
**Автор:** AI Uploader Manager
**Дата:** 2025-11-10

✨ Удачной загрузки!
