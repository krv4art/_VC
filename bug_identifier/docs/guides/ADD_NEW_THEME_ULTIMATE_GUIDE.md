# Ультимейт-гайд: Добавление новой темы в приложение ACS

**Версия:** 3.0
**Дата:** 19 октября 2025
**Статус:** Production Ready ✅

**Обновления в версии 3.0:**
- ✨ Добавлена поддержка адаптивных акцентных цветов для темных тем
- ✨ Каждая светлая тема теперь имеет соответствующую темную версию
- ✨ Автоматическое переключение на соответствующую темную тему с сохранением акцентов

---

## 📋 Содержание

1. [Обзор системы тем](#обзор-системы-тем)
2. [Предварительные требования](#предварительные-требования)
3. [Пошаговая инструкция](#пошаговая-инструкция)
4. [Важные нюансы и подводные камни](#важные-нюансы-и-подводные-камни)
5. [Проверочный список](#проверочный-список)
6. [Рекомендации по цветовым палитрам](#рекомендации-по-цветовым-палитрам)
7. [Тестирование](#тестирование)
8. [Решение проблем](#решение-проблем)

---

## Обзор системы тем

### Архитектура

Приложение использует многоуровневую систему тем:

```
AppThemeType (enum) → AppColors (interface) → ThemeData
                           ↓
              LightColors, DarkColors, CustomColors...
```

### Ключевые файлы

| Файл | Назначение |
|------|-----------|
| `lib/providers/theme_provider_v2.dart` | Управление темами, переключение, сохранение |
| `lib/theme/app_colors.dart` | Цветовые палитры всех тем |
| `lib/theme/app_theme.dart` | Генерация ThemeData из цветов |
| `lib/screens/theme_selection_screen.dart` | UI выбора темы |
| `lib/l10n/app_*.arb` | Локализация названий и описаний тем |

---

## Предварительные требования

### Знания

- Базовое понимание Flutter
- Знание Dart
- Понимание цветовых моделей (HEX, RGB)
- Базовые знания i18n/l10n

### Инструменты

- Flutter SDK 3.32.6+
- IDE (VS Code / Android Studio)
- Git для контроля версий

### Доступные языки для локализации

- 🇬🇧 Английский (en)
- 🇷🇺 Русский (ru)
- 🇺🇦 Украинский (uk)
- 🇪🇸 Испанский (es)
- 🇩🇪 Немецкий (de)
- 🇫🇷 Французский (fr)
- 🇮🇹 Итальянский (it)

---

## Пошаговая инструкция

### Шаг 1: Добавление типа темы в enum

**Файл:** `lib/providers/theme_provider_v2.dart`

**Локация:** Строка 7

```dart
// БЫЛО:
enum AppThemeType {
  light,
  dark,
  ocean,
  darkOcean,
  forest,
  darkForest,
  sunset,
  darkSunset,
}

// СТАЛО (добавляем ОБЯЗАТЕЛЬНО ДВЕ темы - светлую И темную):
enum AppThemeType {
  light,
  dark,
  ocean,
  darkOcean,
  forest,
  darkForest,
  sunset,
  darkSunset,
  sunny,        // ✨ Светлая версия
  darkSunny,    // ✨ Темная версия
}
```

**⚠️ ВАЖНО:**
- Добавляйте **ДВЕ темы одновременно**: светлую и темную версию
- Добавляйте **в конец enum**, чтобы не сломать сохраненные настройки пользователей
- Темная версия должна иметь префикс `dark` (например: `sunny` → `darkSunny`)

---

### Шаг 2: Создание цветовой палитры

**Файл:** `lib/theme/app_colors.dart`

**Локация:** В конец файла, после последнего класса цветов

```dart
/// Sunny Theme (Желтая солнечная тема)
class SunnyColors implements AppColors {
  @override
  final Color saddleBrown = const Color(0xFFFFB300); // Amber

  @override
  final Color naturalGreen = const Color(0xFFFDD835); // Bright Yellow

  @override
  final Color lightGreen = const Color(0xFFFFEE58); // Light Yellow

  @override
  final Color paleGreen = const Color(0xFFFFF9C4); // Pale Yellow

  @override
  final Color deepBrown = const Color(0xFFF57F17); // Deep Yellow

  @override
  final Color mediumBrown = const Color(0xFFFFB300); // Amber

  @override
  final Color background = const Color(0xFFFFFDE7); // Very Light Yellow

  @override
  final Color surface = Colors.white;

  @override
  final Color cardBackground = const Color(0xFFFFFFFF);

  @override
  final Color onBackground = const Color(0xFFF57F17); // Deep Yellow

  @override
  final Color onSurface = const Color(0xFFF57F17); // Deep Yellow

  @override
  final Color onSecondary = const Color(0xFFFFB300); // Amber

  @override
  final Color success = const Color(0xFF4CAF50); // Green

  @override
  final Color warning = const Color(0xFFFF9800); // Orange

  @override
  final Color error = const Color(0xFFF44336); // Red

  @override
  final Color info = const Color(0xFF2196F3); // Blue

  @override
  final Color primary = const Color(0xFFFDD835); // Bright Yellow

  @override
  final Color onPrimary = const Color(0xFF5D4037); // Dark Brown для контраста

  @override
  final Color mediumGrey = const Color(0xFFFFB300);

  @override
  LinearGradient get primaryGradient => LinearGradient(
    colors: [lightGreen, paleGreen],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  @override
  Brightness get brightness => Brightness.light;

  @override
  Color get shadowColor => lightGreen; // Желтая тень

  @override
  Color get appBarColor => primary; // Primary цвет для AppBar

  @override
  Color get navBarColor => surface; // Surface цвет для NavigationBar
}
```

**📝 Примечания:**

- Все свойства интерфейса `AppColors` должны быть реализованы
- Используйте осмысленные комментарии для каждого цвета
- Учитывайте контрастность текста и фона (WCAG AA минимум)

---

### Шаг 2.1: Создание ТЕМНОЙ версии палитры

**Файл:** `lib/theme/app_colors.dart`

**Локация:** Сразу после светлой версии

```dart
/// Dark Sunny Theme (Темная версия солнечной темы)
class DarkSunnyColors implements AppColors {
  // ✨ КЛЮЧЕВОЙ МОМЕНТ: Используем ТЕ ЖЕ АКЦЕНТНЫЕ ЦВЕТА что и в светлой версии!
  @override
  final Color naturalGreen = const Color(0xFFFFEE58); // Light Yellow (как в светлой)

  @override
  final Color lightGreen = const Color(0xFFFFF176); // Light Yellow (как в светлой)

  @override
  final Color paleGreen = const Color(0xFFFFF59D); // Pale Yellow (как в светлой)

  @override
  final Color primary = const Color(0xFFFFF176); // Light Yellow (как в светлой)

  // ⚠️ МЕНЯЕМ ТОЛЬКО ФОНОВЫЕ ЦВЕТА на темные
  @override
  final Color background = const Color(0xFF1E1E1E); // Dark Gray

  @override
  final Color surface = const Color(0xFF2D2D2D); // Darker Gray

  @override
  final Color cardBackground = const Color(0xFF2D2D2D); // Darker Gray

  @override
  final Color onBackground = Colors.white; // Белый текст на темном фоне

  @override
  final Color onSurface = Colors.white; // Белый текст

  // Остальные цвета (можно скопировать из DarkColors):
  @override
  final Color saddleBrown = const Color(0xFFBCAAA4); // Light Brown

  @override
  final Color deepBrown = const Color(0xFF3E2723); // Very Dark Brown

  @override
  final Color mediumBrown = const Color(0xFF4E342E); // Dark Brown

  @override
  final Color onSecondary = const Color(0xFFBCAAA4); // Light Brown

  @override
  final Color success = const Color(0xFF66BB6A);

  @override
  final Color warning = const Color(0xFFFFB74D);

  @override
  final Color error = const Color(0xFFEF5350);

  @override
  final Color info = const Color(0xFF42A5F5);

  @override
  final Color onPrimary = Colors.black; // Темный текст на желтом

  @override
  final Color mediumGrey = const Color(0xFF9E9E9E);

  // ✨ ВАЖНО: Градиент использует ТЕ ЖЕ цвета что и в светлой версии!
  @override
  LinearGradient get primaryGradient => LinearGradient(
    colors: [lightGreen, paleGreen], // Желтый градиент
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  @override
  Brightness get brightness => Brightness.dark; // ⚠️ ОБЯЗАТЕЛЬНО dark!

  @override
  Color get shadowColor => saddleBrown;

  @override
  Color get appBarColor => primary;

  @override
  Color get navBarColor => surface;
}
```

**🎯 Правило создания темной версии:**

| Что меняется | Что НЕ меняется |
|--------------|-----------------|
| ✅ `background` → темный | ❌ `primary` - остается тот же цвет |
| ✅ `surface` → темный | ❌ `naturalGreen` - остается тот же цвет |
| ✅ `onBackground` → белый | ❌ `lightGreen` - остается тот же цвет |
| ✅ `brightness` → `Brightness.dark` | ❌ `paleGreen` - остается тот же цвет |
| | ❌ `primaryGradient` - остается тот же градиент |

**Цель:** Когда пользователь переключится на темный режим, акцентные цвета должны СОХРАНИТЬСЯ, изменится только фон!

---

### Шаг 3: Регистрация темы в провайдере

**Файл:** `lib/providers/theme_provider_v2.dart`

#### 3.1 Метод `_createColorsForTheme` (строка ~28)

```dart
AppColors _createColorsForTheme(AppThemeType theme) {
  switch (theme) {
    case AppThemeType.light:
      return LightColors();
    case AppThemeType.dark:
      return DarkColors();
    case AppThemeType.ocean:
      return OceanColors();
    case AppThemeType.darkOcean:
      return DarkOceanColors();
    case AppThemeType.forest:
      return ForestColors();
    case AppThemeType.darkForest:
      return DarkForestColors();
    case AppThemeType.sunset:
      return SunsetColors();
    case AppThemeType.darkSunset:
      return DarkSunsetColors();
    case AppThemeType.sunny:     // ← Добавить светлую
      return SunnyColors();
    case AppThemeType.darkSunny: // ← Добавить темную
      return DarkSunnyColors();
  }
}
```

#### 3.2 Метод установки темы (после строки ~150)

```dart
/// Установить Sunny тему
Future<void> setSunnyTheme() async {
  await setTheme(AppThemeType.sunny);
}
```

#### 3.3 Метод `getThemeName` (строка ~173)

```dart
String getThemeName(AppThemeType theme) {
  switch (theme) {
    case AppThemeType.light:
      return 'Light';
    case AppThemeType.dark:
      return 'Dark';
    case AppThemeType.ocean:
      return 'Ocean';
    case AppThemeType.darkOcean:
      return 'Dark Ocean';
    case AppThemeType.forest:
      return 'Forest';
    case AppThemeType.darkForest:
      return 'Dark Forest';
    case AppThemeType.sunset:
      return 'Sunset';
    case AppThemeType.darkSunset:
      return 'Dark Sunset';
    case AppThemeType.sunny:     // ← Добавить светлую
      return 'Sunny';
    case AppThemeType.darkSunny: // ← Добавить темную
      return 'Dark Sunny';
  }
}
```

#### 3.4 Метод `_getDarkVariant` - КРИТИЧЕСКИ ВАЖНО! 🔥

Добавьте маппинг светлой темы к её темной версии (после строки ~170):

```dart
/// Получить темную версию светлой темы
AppThemeType _getDarkVariant(AppThemeType lightTheme) {
  switch (lightTheme) {
    case AppThemeType.light:
      return AppThemeType.dark;
    case AppThemeType.ocean:
      return AppThemeType.darkOcean;
    case AppThemeType.forest:
      return AppThemeType.darkForest;
    case AppThemeType.sunset:
      return AppThemeType.darkSunset;
    case AppThemeType.sunny:  // ← Добавить маппинг
      return AppThemeType.darkSunny;
    default:
      return AppThemeType.dark;
  }
}
```

**Зачем это нужно?**
Этот метод используется в `toggleTheme()`, чтобы при переключении на темный режим применялась соответствующая темная версия с **теми же акцентными цветами**!

Например:
- Пользователь выбрал Sunny (желтая) → нажал переключатель dark mode → применяется Dark Sunny (желтая темная)
- Вместо старого поведения: Sunny (желтая) → Dark (зеленая) ❌

#### 3.4 Метод `getThemeIcon` (строка ~189)

```dart
IconData getThemeIcon(AppThemeType theme) {
  switch (theme) {
    case AppThemeType.light:
      return Icons.light_mode;
    case AppThemeType.dark:
      return Icons.dark_mode;
    case AppThemeType.ocean:
      return Icons.water;
    case AppThemeType.forest:
      return Icons.forest;
    case AppThemeType.sunset:
      return Icons.wb_sunny;
    case AppThemeType.sunny:  // ← Добавить
      return Icons.wb_sunny_outlined; // или Icons.sunny
  }
}
```

**⚠️ Выбор иконки:** Убедитесь, что иконка отличается от других тем.

---

### Шаг 4: Обновление UI экрана выбора темы

**Файл:** `lib/screens/theme_selection_screen.dart`

#### 4.1 Метод `_getThemeDisplayName` (строка ~246)

```dart
String _getThemeDisplayName(AppThemeType theme, AppLocalizations l10n) {
  switch (theme) {
    case AppThemeType.light:
      return l10n.lightTheme;
    case AppThemeType.dark:
      return l10n.darkTheme;
    case AppThemeType.ocean:
      return l10n.oceanTheme;
    case AppThemeType.forest:
      return l10n.forestTheme;
    case AppThemeType.sunset:
      return l10n.sunsetTheme;
    case AppThemeType.sunny:  // ← Добавить
      return l10n.sunnyTheme;  // ⚠️ НЕ хардкод!
  }
}
```

**🚨 КРИТИЧЕСКИ ВАЖНО:** Используйте `l10n.sunnyTheme`, а **НЕ** хардкод типа `'Sunny'`!

#### 4.2 Метод `_getThemeDescription` (строка ~263)

```dart
String _getThemeDescription(AppThemeType theme, AppLocalizations l10n) {
  switch (theme) {
    case AppThemeType.light:
      return l10n.lightThemeDescription;
    case AppThemeType.dark:
      return l10n.darkThemeDescription;
    case AppThemeType.ocean:
      return l10n.oceanThemeDescription;
    case AppThemeType.forest:
      return l10n.forestThemeDescription;
    case AppThemeType.sunset:
      return l10n.sunsetThemeDescription;
    case AppThemeType.sunny:  // ← Добавить
      return l10n.sunnyThemeDescription;  // ⚠️ НЕ хардкод!
  }
}
```

---

### Шаг 5: Добавление локализации

Добавьте ключи **во все** языковые файлы:

#### 5.1 Английский (`lib/l10n/app_en.arb`)

```json
{
  ...,
  "sunnyTheme": "Sunny",
  "sunnyThemeDescription": "Bright and cheerful yellow theme"
}
```

#### 5.2 Русский (`lib/l10n/app_ru.arb`)

```json
{
  ...,
  "sunnyTheme": "Солнечная",
  "sunnyThemeDescription": "Яркая и жизнерадостная желтая тема"
}
```

#### 5.3 Украинский (`lib/l10n/app_uk.arb`)

```json
{
  ...,
  "sunnyTheme": "Сонячна",
  "sunnyThemeDescription": "Яскрава та життєрадісна жовта тема"
}
```

#### 5.4 Испанский (`lib/l10n/app_es.arb`)

```json
{
  ...,
  "sunnyTheme": "Soleado",
  "sunnyThemeDescription": "Tema amarillo brillante y alegre"
}
```

#### 5.5 Немецкий (`lib/l10n/app_de.arb`)

```json
{
  ...,
  "sunnyTheme": "Sonnig",
  "sunnyThemeDescription": "Helles und fröhliches gelbes Thema"
}
```

#### 5.6 Французский (`lib/l10n/app_fr.arb`)

```json
{
  ...,
  "sunnyTheme": "Ensoleillé",
  "sunnyThemeDescription": "Thème jaune lumineux et joyeux"
}
```

#### 5.7 Итальянский (`lib/l10n/app_it.arb`)

```json
{
  ...,
  "sunnyTheme": "Soleggiato",
  "sunnyThemeDescription": "Tema giallo luminoso e allegro"
}
```

**📝 Важно:** Ключи должны быть идентичными во всех файлах!

---

### Шаг 6: Генерация локализации

```bash
flutter gen-l10n
```

**⚠️ Критично:** Без этого шага приложение не скомпилируется!

---

### Шаг 7: Сборка и запуск

#### Для полной очистки (рекомендуется):

```bash
flutter clean
flutter pub get
flutter gen-l10n
flutter run
```

#### Для быстрой сборки:

```bash
flutter run
```

**🚨 ВАЖНО:** После изменения локализации нужен **полный перезапуск** (`flutter run`), а не Hot Reload (`r`)!

---

## Важные нюансы и подводные камни

### 🔴 Критические ошибки, которых нужно избежать

#### 1. Хардкод вместо локализации

**❌ НЕПРАВИЛЬНО:**
```dart
case AppThemeType.sunny:
  return 'Sunny';  // Всегда показывает "Sunny"
```

**✅ ПРАВИЛЬНО:**
```dart
case AppThemeType.sunny:
  return l10n.sunnyTheme;  // Показывает перевод
```

#### 2. Использование Hot Reload после изменения локализации

**❌ НЕПРАВИЛЬНО:**
- Внесли изменения в `.arb` файлы
- Запустили `flutter gen-l10n`
- Нажали `R` (Hot Restart)
- Локализация не обновилась ❌

**✅ ПРАВИЛЬНО:**
- Внесли изменения в `.arb` файлы
- Запустили `flutter gen-l10n`
- Остановили приложение (`q`)
- Запустили заново `flutter run`
- Локализация обновилась ✅

#### 3. Забыли добавить тему в enum

Если добавили `SunnyColors`, но забыли добавить `sunny` в enum `AppThemeType`, код не скомпилируется.

#### 4. Неполная реализация интерфейса

Все свойства из `AppColors` должны быть реализованы:
- `saddleBrown`, `naturalGreen`, `lightGreen`, `paleGreen`
- `deepBrown`, `mediumBrown`
- `background`, `surface`, `cardBackground`
- `onBackground`, `onSurface`, `onSecondary`
- `success`, `warning`, `error`, `info`
- `primary`, `onPrimary`, `mediumGrey`
- `primaryGradient`, `brightness`, `shadowColor`
- `appBarColor`, `navBarColor`

#### 5. Пропуск языков в локализации

Если добавили перевод только в `app_en.arb` и `app_ru.arb`, но забыли про остальные 5 языков, `flutter gen-l10n` выдаст ошибку.

---

### 🟡 Важные особенности

#### 1. Порядок тем в enum имеет значение

Flutter сохраняет выбранную тему по **индексу** в enum. Если изменить порядок, пользователи получат другую тему после обновления.

**Правило:** Всегда добавляйте новые темы **в конец**.

#### 2. Веб-версия и иконки

Для веб-версии Flutter необходимо добавить Material Icons в `web/index.html`:

```html
<!-- Material Icons -->
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
<link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
```

Без этого иконки не будут отображаться в браузере.

#### 3. Brightness влияет на поведение

```dart
@override
Brightness get brightness => Brightness.light;  // или Brightness.dark
```

Это свойство влияет на:
- Цвет системных элементов (StatusBar, NavigationBar)
- Логику переключения между светлой/темной темой
- Автоматический выбор цветов для некоторых виджетов

#### 4. Контрастность цветов

Убедитесь, что соблюдаются стандарты WCAG для контрастности:
- **AA уровень:** минимум 4.5:1 для обычного текста
- **AAA уровень:** минимум 7:1 для обычного текста

Инструмент для проверки: https://webaim.org/resources/contrastchecker/

---

## Проверочный список

Используйте этот чеклист для проверки полноты внедрения:

### Код

- [ ] Добавлен новый тип в `AppThemeType` enum
- [ ] Создан класс цветовой палитры (implements `AppColors`)
- [ ] Все свойства интерфейса `AppColors` реализованы
- [ ] Обновлен метод `_createColorsForTheme`
- [ ] Добавлен метод `set[ThemeName]Theme()`
- [ ] Обновлен метод `getThemeName`
- [ ] Обновлен метод `getThemeIcon`
- [ ] Обновлен метод `_getThemeDisplayName` (используется `l10n`, не хардкод!)
- [ ] Обновлен метод `_getThemeDescription` (используется `l10n`, не хардкод!)

### Локализация

- [ ] Добавлены ключи в `app_en.arb`
- [ ] Добавлены ключи в `app_ru.arb`
- [ ] Добавлены ключи в `app_uk.arb`
- [ ] Добавлены ключи в `app_es.arb`
- [ ] Добавлены ключи в `app_de.arb`
- [ ] Добавлены ключи в `app_fr.arb`
- [ ] Добавлены ключи в `app_it.arb`
- [ ] Запущена команда `flutter gen-l10n`

### Сборка и тестирование

- [ ] Код успешно компилируется
- [ ] Приложение запускается без ошибок
- [ ] Тема появляется в списке выбора
- [ ] Переводы работают на всех языках
- [ ] Иконка темы отображается корректно
- [ ] Цвета применяются правильно на всех экранах
- [ ] Текст читаем на всех фонах
- [ ] Выбор темы сохраняется после перезапуска

### Веб-версия (опционально)

- [ ] Material Icons добавлены в `web/index.html`
- [ ] Шрифты загружаются корректно
- [ ] Иконки отображаются в браузере

---

## Рекомендации по цветовым палитрам

### Подбор цветов

#### 1. Определите основной цвет (Primary)

Это главный акцентный цвет темы. Примеры:
- Солнечная тема: `#FDD835` (ярко-желтый)
- Океан тема: `#00ACC1` (циан)
- Лесная тема: `#558B2F` (лесной зеленый)

#### 2. Создайте оттенки

На основе primary создайте:
- **Light variant** (более светлый): primary + 20% яркости
- **Dark variant** (более темный): primary - 20% яркости
- **Pale variant** (очень светлый): primary + 40% яркости

Инструмент: https://material.io/design/color/the-color-system.html

#### 3. Выберите цвет фона

- **Светлые темы:** `#FFFFFF` или очень светлый оттенок primary (95%+ светлоты)
- **Темные темы:** `#121212` - `#1E1E1E`

#### 4. Определите цвета текста (on-colors)

Для хорошей читаемости:
- На светлом фоне: темный текст (`#212121`, `#424242`)
- На темном фоне: светлый текст (`#FFFFFF`, `#FAFAFA`)
- На цветном фоне: проверьте контрастность!

#### 5. Семантические цвета

Обычно остаются стандартными:
- **Success:** `#4CAF50` (зеленый)
- **Warning:** `#FF9800` (оранжевый)
- **Error:** `#F44336` (красный)
- **Info:** `#2196F3` (синий)

Но можно адаптировать под тему, сохраняя узнаваемость.

### Примеры цветовых схем

#### Теплые тона (Закат, Солнце)

```dart
primary: #FF9800    // Оранжевый
light: #FFB74D      // Светло-оранжевый
pale: #FFE0B2       // Бледно-оранжевый
background: #FFF3E0 // Очень светлый оранжевый
```

#### Холодные тона (Океан, Лед)

```dart
primary: #00ACC1    // Циан
light: #4DD0E1      // Светло-циан
pale: #B2EBF2       // Бледно-циан
background: #E0F7FA // Очень светлый циан
```

#### Природные тона (Лес, Земля)

```dart
primary: #558B2F    // Лесной зеленый
light: #7CB342      // Светло-зеленый
pale: #C5E1A5       // Бледно-зеленый
background: #F1F8E9 // Очень светлый зеленый
```

### Инструменты для подбора цветов

- **Material Design Color Tool:** https://material.io/resources/color/
- **Adobe Color:** https://color.adobe.com/
- **Coolors:** https://coolors.co/
- **Paletton:** https://paletton.com/

---

## Тестирование

### Функциональное тестирование

#### 1. Выбор темы

- [ ] Откройте экран выбора темы
- [ ] Убедитесь, что новая тема в списке
- [ ] Выберите тему
- [ ] Проверьте, что предпросмотр применяется сразу
- [ ] Нажмите "Сохранить"
- [ ] Убедитесь, что тема применена

#### 2. Сохранение выбора

- [ ] Выберите новую тему
- [ ] Полностью закройте приложение
- [ ] Откройте приложение снова
- [ ] Проверьте, что выбранная тема загрузилась

#### 3. Проверка на всех экранах

Проверьте тему на каждом экране приложения:
- [ ] Главный экран (Homepage)
- [ ] Экран сканирования
- [ ] История сканирований
- [ ] Профиль
- [ ] Настройки
- [ ] Чат с AI
- [ ] Результаты анализа
- [ ] Paywall

#### 4. Тестирование локализации

Для каждого языка:
- [ ] Переключите язык
- [ ] Откройте экран выбора темы
- [ ] Проверьте название темы
- [ ] Проверьте описание темы

### Визуальное тестирование

#### 1. Читаемость текста

- [ ] Заголовки (h1, h2, h3, h4)
- [ ] Обычный текст (body, bodySmall)
- [ ] Текст на кнопках
- [ ] Подписи (caption)
- [ ] Текст в input полях
- [ ] Текст ошибок

#### 2. Контрастность

Используйте инструмент проверки контрастности:
- [ ] Текст на фоне
- [ ] Текст на карточках
- [ ] Текст на кнопках
- [ ] Иконки

#### 3. Визуальная согласованность

- [ ] Все элементы используют цвета темы
- [ ] Градиенты применяются корректно
- [ ] Тени выглядят естественно
- [ ] Границы элементов видны
- [ ] Состояния кнопок (нажата, отключена) различимы

### Тестирование на разных платформах

- [ ] Android
- [ ] iOS (если доступно)
- [ ] Web (Chrome)
- [ ] Web (Firefox)
- [ ] Web (Safari)

---

## Решение проблем

### Проблема 1: Тема не появляется в списке

**Симптомы:**
- В экране выбора темы новая тема отсутствует

**Причины:**
- Не добавлен тип в enum `AppThemeType`
- Не запущена команда `flutter gen-l10n`
- Не перезапущено приложение

**Решение:**
```bash
# Проверьте enum
# Убедитесь, что новый тип добавлен

# Перегенерируйте локализацию
flutter gen-l10n

# Полный перезапуск
flutter run
```

---

### Проблема 2: Название темы не переводится

**Симптомы:**
- При смене языка название темы остается на одном языке
- Название всегда одинаковое

**Причина:**
- Используется хардкод вместо `l10n`

**Решение:**

Найдите в `theme_selection_screen.dart`:

```dart
// НЕПРАВИЛЬНО:
case AppThemeType.sunny:
  return 'Sunny';

// ПРАВИЛЬНО:
case AppThemeType.sunny:
  return l10n.sunnyTheme;
```

После исправления:
```bash
flutter run  # Полный перезапуск!
```

---

### Проблема 3: Ошибка компиляции "undefined name"

**Симптомы:**
```
Error: Getter not found: 'sunnyTheme'
```

**Причины:**
- Не добавлены ключи в `.arb` файлы
- Не запущена команда `flutter gen-l10n`
- Ключи написаны с ошибкой

**Решение:**
```bash
# 1. Проверьте все .arb файлы
# Убедитесь, что ключи добавлены и написаны одинаково

# 2. Перегенерируйте локализацию
flutter gen-l10n

# 3. Если ошибка осталась, очистите проект
flutter clean
flutter pub get
flutter gen-l10n
```

---

### Проблема 4: Иконки не отображаются (веб-версия)

**Симптомы:**
- В браузере вместо иконок пустые квадраты
- Иконки показывают "□"

**Причина:**
- Не подключены Material Icons в `web/index.html`

**Решение:**

Откройте `web/index.html` и добавьте:

```html
<head>
  ...
  <!-- Material Icons -->
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
  <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
  ...
</head>
```

Затем:
```bash
flutter run -d chrome
```

---

### Проблема 5: Цвета применяются неправильно

**Симптомы:**
- Некоторые элементы не используют цвета темы
- Цвета "смешиваются" с другими темами

**Причины:**
- Не все свойства `AppColors` реализованы
- Где-то используется хардкод цветов
- Не все виджеты используют `context.colors`

**Решение:**

1. Убедитесь, что все свойства интерфейса реализованы:
```dart
class YourThemeColors implements AppColors {
  // Должны быть ВСЕ свойства из AppColors
}
```

2. В виджетах используйте:
```dart
// НЕПРАВИЛЬНО:
color: Color(0xFF4CAF50)

// ПРАВИЛЬНО:
color: context.colors.primary
```

---

### Проблема 6: После `flutter clean` пропали все иконки

**Симптомы:**
- После `flutter clean` приложение не показывает иконки

**Причина:**
- Это нормальное поведение. `flutter clean` удаляет все скомпилированные файлы.

**Решение:**
```bash
# Просто пересоберите приложение
flutter run
```

Иконки вернутся после сборки.

---

### Проблема 7: Тема не сохраняется после перезапуска

**Симптомы:**
- Выбрали тему, закрыли приложение
- После открытия - опять стандартная тема

**Причины:**
- Проблема с `SharedPreferences`
- Неправильный порядок в enum (изменили индексы)

**Решение:**

1. Проверьте логи на ошибки сохранения
2. Убедитесь, что не меняли порядок в enum
3. Попробуйте очистить данные:

```dart
// В провайдере
final prefs = await SharedPreferences.getInstance();
await prefs.clear();
```

---

## Дополнительные ресурсы

### Документация проекта

- `docs/THEME_GUIDE.md` - Общее руководство по темам
- `docs/MULTI_THEME_GUIDE.md` - Работа с множественными темами
- `docs/LOCALIZATION_GUIDE.md` - Руководство по локализации
- `docs/COMMON_ISSUES.md` - Частые проблемы

### Внешние ресурсы

- [Flutter Theming](https://docs.flutter.dev/cookbook/design/themes)
- [Material Design Color System](https://material.io/design/color/)
- [Flutter Localization](https://docs.flutter.dev/development/accessibility-and-localization/internationalization)
- [WCAG Contrast Guidelines](https://www.w3.org/WAI/WCAG21/Understanding/contrast-minimum.html)

---

## История изменений

### Версия 2.0 (12.10.2025)
- ✅ Добавлен полный checklist
- ✅ Добавлены решения распространенных проблем
- ✅ Добавлены примеры цветовых схем
- ✅ Добавлена информация о веб-версии и иконках
- ✅ Добавлены критические предупреждения о хардкоде
- ✅ Добавлены инструкции по тестированию

### Версия 1.0 (первоначальная)
- Базовая инструкция по добавлению темы

---

## Контакты и поддержка

При возникновении проблем:
1. Проверьте раздел "Решение проблем"
2. Изучите документацию в `docs/`
3. Проверьте логи приложения
4. Создайте Issue в репозитории

---

**Удачи в создании красивых тем! 🎨✨**
