# Руководство по добавлению нового языка в приложение

## Введение

В данном проекте используется система локализации Flutter (Flutter Localizations) для поддержки мультиязычности. Приложение в настоящее время поддерживает 9 языков: английский (en), русский (ru), украинский (uk), испанский (es), немецкий (de), французский (fr), итальянский (it), корейский (ko) и арабский (ar).

Система локализации основана на файлах формата ARB (Application Resource Bundle), которые содержат пары "ключ-значение" для всех текстовых строк в приложении. На основе этих файлов автоматически генерируются классы локализации.

**Важно:** Кроме стандартной локализации UI, в приложении также используется система управления промптами для AI-ассистента. При добавлении нового языка необходимо также перевести промпты в файле [`assets/config/prompts.json`](assets/config/prompts.json).

## Предварительные требования

Перед добавлением нового языка убедитесь что у вас есть:

1. Базовое понимание формата JSON
2. Знание языка, который вы добавляете
3. Установленный Flutter SDK
4. Доступ к кодовой базе проекта

## Пошаговое руководство по добавлению нового языка

### Шаг 1: Создание файла .arb с переводами

1.1. Скопируйте существующий файл ARB (например, [`app_en.arb`](lib/l10n/app_en.arb:1)) и создайте его версию для нового языка.

```bash
# Пример для добавления французского языка (fr)
cp lib/l10n/app_en.arb lib/l10n/app_fr.arb
```

1.2. Переименуйте файл в соответствии с кодом языка:
- `app_de.arb` для немецкого (de)
- `app_fr.arb` для французского (fr)
- `app_it.arb` для итальянского (it)
- `app_pl.arb` для польского (pl)
- и т.д.

1.3. Откройте созданный файл и переведите все значения на новый язык, сохранив ключи без изменений.

```json
{
  "skinAnalysis": "Анализ кожи",
  "checkYourCosmetics": "Проверьте свои косметические средства",
  "startScanning": "Начать сканирование",
  // ... и так далее для всех строк
}
```

### Шаг 2: Создание класса локализации

2.1. Скопируйте существующий класс локализации (например, [`app_localizations_en.dart`](lib/l10n/app_localizations_en.dart:1)):

```bash
cp lib/l10n/app_localizations_en.dart lib/l10n/app_localizations_fr.dart
```

2.2. Откройте созданный файл и внесите следующие изменения:

- Измените имя класса (например, с `AppLocalizationsEn` на `AppLocalizationsFr`)
- Измените код локали в конструкторе (например, с `'en'` на `'fr'`)
- Обновите комментарий в начале файла с указанием нового языка

```dart
// ignore: unused_import
import 'package:intl/intl.dart' as intl;
import 'app_localizations.dart';

// ignore_for_file: type=lint

/// The translations for French (`fr`).
class AppLocalizationsFr extends AppLocalizations {
  AppLocalizationsFr([String locale = 'fr']) : super(locale);

  @override
  String get skinAnalysis => 'Analyse de la peau';
  
  // ... и так далее для всех методов
}
```

### ⚠️ Шаг 3: КРИТИЧЕСКИ ВАЖНО - Обновление supportedLocales в main.dart

> **ВНИМАНИЕ:** Этот шаг является ОБЯЗАТЕЛЬНЫМ и его пропуск приведет к тому, что новый язык не будет работать в приложении!

**Проблема:** Если вы пропустите этот шаг, даже при наличии всех файлов локализации, новый язык не будет отображаться в приложении. Вместо этого интерфейс останется на языке по умолчанию (обычно английском).

3.1. Откройте файл [`main.dart`](lib/main.dart:1).

3.2. Найдите секцию `supportedLocales` (строки 82-89) и добавьте новую локаль в список:

```dart
supportedLocales: const [
  Locale('en', ''), // English
  Locale('ru', ''), // Russian
  Locale('uk', ''), // Ukrainian
  Locale('es', ''), // Spanish
  Locale('de', ''), // German
  Locale('fr', ''), // French  // Добавленная строкa
],
```

3.3. Убедитесь, что код языка соответствует коду, который вы использовали в файлах .arb и классах локализации.

> **ПОЧЕМУ ЭТО КРИТИЧЕСКИ ВАЖНО:** Flutter использует список `supportedLocales` для определения, какие языки доступны в приложении. Если язык не указан в этом списке, система не сможет его распознать, даже если все файлы локализации существуют.

### Шаг 4: Обновление app_localizations.dart

4.1. Откройте файл [`app_localizations.dart`](lib/l10n/app_localizations.dart:1).

4.2. Добавьте импорт для нового класса локализации в секции импортов (строки 8-12):

```dart
import 'app_localizations_de.dart';
import 'app_localizations_en.dart';
import 'app_localizations_es.dart';
import 'app_localizations_fr.dart';  // Добавленная строка
import 'app_localizations_it.dart';
import 'app_localizations_ko.dart';
import 'app_localizations_ru.dart';
import 'app_localizations_uk.dart';
```

4.3. Добавьте новую локаль в список `supportedLocales` (строки 96-102):

```dart
static const List<Locale> supportedLocales = <Locale>[
  Locale('ar'),
  Locale('de'),
  Locale('en'),
  Locale('es'),
  Locale('fr'),  // Добавленная строка
  Locale('it'),
  Locale('ko'),
  Locale('ru'),
  Locale('uk')
];
```

4.4. Обновите метод `isSupported` в классе `_AppLocalizationsDelegate` (строка 1266):

```dart
@override
bool isSupported(Locale locale) => <String>['ar', 'de', 'en', 'es', 'fr', 'it', 'ko', 'ru', 'uk'].contains(locale.languageCode);
```

4.5. Добавьте новый случай в функцию `lookupAppLocalizations` (строки 1276-1282):

```dart
switch (locale.languageCode) {
  case 'ar': return AppLocalizationsAr();
  case 'de': return AppLocalizationsDe();
  case 'en': return AppLocalizationsEn();
  case 'es': return AppLocalizationsEs();
  case 'fr': return AppLocalizationsFr();  // Добавленная строка
  case 'it': return AppLocalizationsIt();
  case 'ko': return AppLocalizationsKo();
  case 'ru': return AppLocalizationsRu();
  case 'uk': return AppLocalizationsUk();
}
```

### Шаг 5: Добавление языка в language_screen.dart

5.1. Откройте файл [`language_screen.dart`](lib/screens/language_screen.dart:1).

5.2. Добавьте новый язык в список `displayLanguages` (строки 44-51):

```dart
final List<Map<String, String>> displayLanguages = [
  {'code': 'en', 'flag': '🇺🇸'},
  {'code': 'ru', 'flag': '🇷🇺'},
  {'code': 'uk', 'flag': '🇺🇦'},
  {'code': 'es', 'flag': '🇪🇸'},
  {'code': 'de', 'flag': '🇩🇪'},
  {'code': 'fr', 'flag': '🇫🇷'},  // Добавленная строка
  {'code': 'it', 'flag': '🇮🇹'},
  {'code': 'ko', 'flag': '🇰🇷'},
  {'code': 'ar', 'flag': '🇸🇦'},
];
```

5.3. Добавьте новый случай в функцию `getLanguageName` (строки 53-68):

```dart
String getLanguageName(String code) {
  switch (code) {
    case 'en':
      return l10n.language_en;
    case 'ru':
      return l10n.language_ru;
    case 'uk':
      return l10n.language_uk;
    case 'es':
      return l10n.language_es;
    case 'de':
      return l10n.language_de;
    case 'fr':  // Добавленная строка
      return l10n.language_fr;  // Добавленная строка
    case 'it':
      return l10n.language_it;
    case 'ko':
      return l10n.language_ko;
    case 'ar':
      return l10n.language_ar;
    default:
      return code;
  }
}
```

### Шаг 6: Добавление названия нового языка во все существующие .arb файлы

6.1. В каждый файл .arb (включая тот, что вы создали на шаге 1) добавьте запись для названия нового языка:

```json
// В app_en.arb
"language_fr": "French",

// В app_ru.arb
"language_fr": "Французский",

// В app_uk.arb
"language_fr": "Французська",

// В app_es.arb
"language_fr": "Francés",

// В app_de.arb
"language_fr": "Französisch",

// В app_fr.arb
"language_fr": "Français",
```

### ⚠️ Шаг 7: КРИТИЧЕСКИ ВАЖНО - Локализация промптов AI

> **ВНИМАНИЕ:** Этот шаг является ОБЯЗАТЕЛЬНЫМ для полноценной работы AI-ассистента на новом языке!

**Проблема:** Если вы пропустите этот шаг, AI-ассистент будет продолжать отвечать на английском языке или на языке по умолчанию, даже если весь интерфейс приложения переведен на новый язык.

7.1. Откройте файл [`assets/config/prompts.json`](assets/config/prompts.json).

7.2. Добавьте переводы для всех типов промптов на новый язык. Найдите следующие секции и добавьте переводы:

```json
{
  "chat": {
    "system_prompts": {
      "default": "You are a helpful AI assistant specializing in cosmetics and skincare.",
      "cosmetic_expert": "You are a professional cosmetic chemist and skincare expert."
    },
    "language_instructions": {
      "ar": "مهم: قدم الإجابة بأكملها باللغة العربية. يجب أن تكون جميع الأوصاف والتحذيرات والتوصيات باللغة العربية.",
      "ru": "ВАЖНО: Предоставь весь ответ на РУССКОМ языке. Все описания, предупреждения и рекомендации должны быть на русском языке.",
      "uk": "ВАЖЛИВО: Надай всю відповідь УКРАЇНСЬКОЮ мовою. Усі описи, попередження та рекомендації мають бути українською мовою.",
      "es": "IMPORTANTE: Proporciona toda la respuesta en ESPAÑOL. Todas las descripciones, advertencias y recomendaciones deben estar en español.",
      "en": "IMPORTANT: Provide the entire response in ENGLISH. All descriptions, warnings, and recommendations should be in English.",
      "de": "WICHTIG: Geben Sie die gesamte Antwort auf DEUTSCH. Alle Beschreibungen, Warnungen und Empfehlungen müssen auf Deutsch sein.",
      "fr": "IMPORTANT: Fournissez toute la réponse en FRANÇAIS. Toutes les descriptions, avertissements et recommandations doivent être en français.", // Добавленная строка
      "it": "IMPORTANTE: Fornisci l'intera risposta in ITALIANO. Tutte le descrizioni, avvertenze e raccomandazioni devono essere in italiano.",
      "ko": "중요: 전체 응답을 한국어로 제공하십시오. 모든 설명, 경고 및 권장 사항은 한국어여야 합니다."
    },
    "welcome_messages": {
      "ar": "مرحباً! أنا مساعد الذكاء الاصطناعي المتخصص في مستحضرات التجميل. كيف يمكنني مساعدتك؟",
      "ru": "Привет! Я ваш AI-ассистент по косметике. Чем могу помочь?",
      "uk": "Привіт! Я ваш AI-асистент з косметики. Чим можу допомогти?",
      "es": "¡Hola! Soy tu asistente de IA especializado en cosmética. ¿En qué puedo ayudarte?",
      "en": "Hello! I'm your AI assistant specializing in cosmetics. How can I help you?",
      "de": "Hallo! Ich bin Ihr KI-Assistent für Kosmetik. Wie kann ich Ihnen helfen?",
      "fr": "Bonjour ! Je suis votre assistant IA spécialisé en cosmétiques. Comment puis-je vous aider ?", // Добавленная строка
      "it": "Ciao! Sono il tuo assistente IA specializzato in cosmetica. Come posso aiutarti?",
      "ko": "안녕하세요! 저는 화장품 전문 AI 어시스턴트입니다. 어떻게 도와드릴까요?"
    },
    "error_messages": {
      "ar": "عذراً، حدث خطأ. يرجى المحاولة مرة أخرى لاحقاً.",
      "ru": "Извините, произошла ошибка. Попробуйте позже.",
      "uk": "Вибачте, сталася помилка. Спробуйте пізніше.",
      "es": "Lo siento, ha ocurrido un error. Inténtelo más tarde.",
      "en": "Sorry, an error occurred. Please try again later.",
      "de": "Entschuldigung, es ist ein Fehler aufgetreten. Bitte versuchen Sie es später erneut.",
      "fr": "Désolé, une erreur s'est produite. Veuillez réessayer plus tard.", // Добавленная строка
      "it": "Scusi, si è verificato un errore. Riprova più tardi.",
      "ko": "죄송합니다. 오류가 발생했습니다. 나중에 다시 시도해 주세요."
    },
    "context_messages": {
      "scan_results": {
        "ar": "أرى نتائج فحصك. يمكنك طرح أي أسئلة حول المكونات أو مخاوف السلامة أو التوصيات.",
        "ru": "Я вижу результаты вашего сканирования. Можете задавать любые вопросы об ингредиентах, безопасности или рекомендациях.",
        "uk": "Я бачу результати вашого сканування. Можете ставити будь-які питання про інгредієнти, безпеку або рекомендації.",
        "es": "Veo los resultados de tu escaneo. Puedes hacer cualquier pregunta sobre ingredientes, preocupaciones de seguridad o recomendaciones.",
        "en": "I can see your scan results. Feel free to ask me any questions about ingredients, safety concerns, or recommendations.",
        "de": "Ich kann Ihre Scanergebnisse sehen. Fühlen Sie sich frei, mir Fragen zu den Inhaltsstoffen, Sicherheitsbedenken oder Empfehlungen zu stellen.",
        "fr": "Je vois les résultats de votre analyse. N'hésitez pas à me poser des questions sur les ingrédients, les problèmes de sécurité ou les recommandations.", // Добавленная строка
        "it": "Vedo i risultati della tua scansione. Sentiti libero di farmi domande sugli ingredienti, preoccupazioni sulla sicurezza o raccomandazioni.",
        "ko": "스캔 결과를 볼 수 있습니다. 성분, 안전 우려 또는 권장 사항에 대해 어떤 질문이든 자유롭게 물어보세요."
      }
    }
  }
}
```

7.3. Обязательно добавьте переводы для ВСЕХ четырех секций промптов:
- **language_instructions** - инструкции для AI о языке ответа
- **welcome_messages** - приветственные сообщения в чате
- **error_messages** - сообщения об ошибках
- **context_messages.scan_results** - контекстные сообщения для результатов сканирования

> **ПОЧЕМУ ЭТО КРИТИЧЕСКИ ВАЖНО:** Промпты определяют, на каком языке AI-ассистент будет отвечать пользователю. Без правильных промптов AI будет продолжать общаться на английском языке, даже если весь интерфейс переведен.

### Шаг 8: Создание материалов для store listings

8.1. Создайте папку для нового языка в директории [`store_listings/shared/`](store_listings/shared/):

```bash
mkdir store_listings/shared/fr
```

8.2. Скопируйте и переведите все файлы для store listings:

```bash
# Общие файлы (для обоих сторов)
cp store_listings/shared/en/name.txt store_listings/shared/fr/name.txt
cp store_listings/shared/en/description.txt store_listings/shared/fr/description.txt
cp store_listings/shared/en/keywords.txt store_listings/shared/fr/keywords.txt

# Специфичные для App Store
cp store_listings/shared/en/subtitle_app_store.txt store_listings/shared/fr/subtitle_app_store.txt

# Специфичные для Google Play
cp store_listings/shared/en/short_description_google_play.txt store_listings/shared/fr/short_description_google_play.txt
cp store_listings/shared/en/promo_text_google_play.txt store_listings/shared/fr/promo_text_google_play.txt
```

8.3. Переведите содержимое всех файлов на новый язык.

**Важно:** Убедитесь, что соблюдаете ограничения по количеству символов для каждого типа файла:
- `name.txt`: max 30 символов (App Store), max 50 символов (Google Play)
- `subtitle_app_store.txt`: max 30 символов
- `keywords.txt`: max 100 символов (через запятую, без пробелов)
- `short_description_google_play.txt`: max 80 символов
- `promo_text_google_play.txt`: max 170 символов
- `description.txt`: max 4000 символов

### Шаг 9: Регенерация файлов локализации

9.1. Выполните команду для регенерации файлов локализации:

```bash
flutter gen-l10n
```

Эта команда прочитает все файлы .arb и сгенерирует необходимые классы локализации на их основе.

## ⚠️ Частые проблемы и их решения

### ⚠️ ПРОБЛЕМА 1: Новый язык не работает в приложении

**Симптомы:** Вы добавили все файлы локализации, но новый язык не отображается в приложении. Интерфейс остается на языке по умолчанию (обычно английском).

**Причина:** **Самая частая ошибка** - вы забыли добавить новую локаль в список `supportedLocales` в файле [`main.dart`](lib/main.dart:82-89).

**Решение:**
1. Откройте файл [`main.dart`](lib/main.dart:1)
2. Найдите секцию `supportedLocales`
3. Добавьте новую локаль в список:
```dart
supportedLocales: const [
  Locale('en', ''), // English
  Locale('ru', ''), // Russian
  Locale('uk', ''), // Ukrainian
  Locale('es', ''), // Spanish
  Locale('de', ''), // German
  Locale('fr', ''), // French  // Добавьте эту строку
],
```

> **ВАЖНО:** Этот шаг является КРИТИЧЕСКИ ВАЖНЫМ. Без него новый язык не будет работать, даже если все остальные файлы локализации созданы правильно.

### Ошибка 2: Отсутствие перевода для нового языка в app_localizations.dart

**Проблема:** После добавления нового языка приложение не компилируется с ошибкой о отсутствующем классе локализации.

**Решение:** Убедитесь, что вы правильно добавили импорт нового класса локализации и добавили соответствующий случай в функцию `lookupAppLocalizations`.

### Ошибка 3: Несоответствие ключей в файлах .arb

**Проблема:** Некоторые строки отображаются на английском языке вместо нового языка.

**Решение:** Убедитесь, что все ключи в вашем новом файле .arb точно соответствуют ключам в основном файле app_en.arb. Даже небольшое расхождение в названии ключа вызовет проблему.

### Ошибка 4: Отсутствие названия нового языка в других .arb файлах

**Проблема:** В экране выбора языка название нового языка отображается как код языка (например, "fr" вместо "Français").

**Решение:** Убедитесь, что вы добавили запись `language_XX` (где XX - код языка) во все файлы .arb, включая основной.

### Ошибка 5: Несоответствие кода языка в названии файла

**Проблема:** Файл локализации не распознается системой.

**Решение:** Убедитесь, что код языка в названии файла .arb и класса локализации соответствует стандарту ISO 639-1 (двухбуквенный код языка).

### Ошибка 6: Отсутствие флага для нового языка

**Проблема:** В экране выбора языка нет флага для нового языка.

**Решение:** Добавьте соответствующий флаг в список `displayLanguages` в файле `language_screen.dart`. Используйте соответствующий эмодзи флага.

## Проверка работоспособности

После выполнения всех шагов:

1. Пересоберите приложение:
   ```bash
   flutter clean
   flutter pub get
   flutter run
   ```

2. Перейдите в экран выбора языка и убедитесь, что новый язык отображается в списке.

3. Выберите новый язык и проверьте, что все элементы интерфейса переключились на этот язык.

4. Проверьте различные разделы приложения, чтобы убедиться, что все строки переведены корректно.

5. Проверьте работу AI-ассистента на новом языке.

6. Убедитесь, что все файлы в [`store_listings/shared/{lang}/`](store_listings/shared/) переведены и соответствуют ограничениям по длине.

## Примеры кода для каждого шага

### Пример создания файла .arb для немецкого языка

```json
{
  "skinAnalysis": "Hautanalyse",
  "checkYourCosmetics": "Überprüfen Sie Ihre Kosmetik",
  "startScanning": "Scannen starten",
  "language_de": "Deutsch",
  // ... остальные переводы
}
```

### Пример создания класса локализации для немецкого языка

```dart
// ignore: unused_import
import 'package:intl/intl.dart' as intl;
import 'app_localizations.dart';

// ignore_for_file: type=lint

/// The translations for German (`de`).
class AppLocalizationsDe extends AppLocalizations {
  AppLocalizationsDe([String locale = 'de']) : super(locale);

  @override
  String get skinAnalysis => 'Hautanalyse';

  @override
  String get checkYourCosmetics => 'Überprüfen Sie Ihre Kosmetik';

  @override
  String get startScanning => 'Scannen starten';

  @override
  String get language_de => 'Deutsch';
  
  // ... остальные методы
}
```

### Пример обновления app_localizations.dart

```dart
import 'app_localizations_ar.dart';
import 'app_localizations_de.dart';
import 'app_localizations_en.dart';
import 'app_localizations_es.dart';
import 'app_localizations_fr.dart';
import 'app_localizations_it.dart';
import 'app_localizations_ko.dart';
import 'app_localizations_ru.dart';
import 'app_localizations_uk.dart';

// ...

static const List<Locale> supportedLocales = <Locale>[
  Locale('ar'),
  Locale('de'),
  Locale('en'),
  Locale('es'),
  Locale('fr'),
  Locale('it'),
  Locale('ko'),
  Locale('ru'),
  Locale('uk')
];

// ...

AppLocalizations lookupAppLocalizations(Locale locale) {
  switch (locale.languageCode) {
    case 'ar': return AppLocalizationsAr();
    case 'de': return AppLocalizationsDe();
    case 'en': return AppLocalizationsEn();
    case 'es': return AppLocalizationsEs();
    case 'fr': return AppLocalizationsFr();
    case 'it': return AppLocalizationsIt();
    case 'ko': return AppLocalizationsKo();
    case 'ru': return AppLocalizationsRu();
    case 'uk': return AppLocalizationsUk();
  }
  
  throw FlutterError(
    'AppLocalizations.delegate failed to load unsupported locale "$locale"...'
  );
}
```

## Структура файлов

```
lib/
├── l10n/
│   ├── app_ar.arb               # Файл переводов для арабского языка
│   ├── app_de.arb               # Файл переводов для немецкого языка
│   ├── app_en.arb               # Файл переводов для английского языка
│   ├── app_es.arb               # Файл переводов для испанского языка
│   ├── app_fr.arb               # Файл переводов для французского языка
│   ├── app_it.arb               # Файл переводов для итальянского языка
│   ├── app_ko.arb               # Файл переводов для корейского языка
│   ├── app_ru.arb               # Файл переводов для русского языка
│   ├── app_uk.arb               # Файл переводов для украинского языка
│   ├── app_localizations.dart   # Основной класс локализации
│   ├── app_localizations_ar.dart # Класс локализации для арабского языка
│   ├── app_localizations_de.dart # Класс локализации для немецкого языка
│   ├── app_localizations_en.dart # Класс локализации для английского языка
│   ├── app_localizations_es.dart # Класс локализации для испанского языка
│   ├── app_localizations_fr.dart # Класс локализации для французского языка
│   ├── app_localizations_it.dart # Класс локализации для итальянского языка
│   ├── app_localizations_ko.dart # Класс локализации для корейского языка
│   ├── app_localizations_ru.dart # Класс локализации для русского языка
│   └── app_localizations_uk.dart # Класс локализации для украинского языка
├── main.dart                    # Главный файл приложения
└── screens/
    └── language_screen.dart     # Экран выбора языка

store_listings/
└── shared/
    ├── ar/                     # Материалы для арабского языка
    ├── de/                     # Материалы для немецкого языка
    ├── en/                     # Материалы для английского языка
    ├── es/                     # Материалы для испанского языка
    ├── fr/                     # Материалы для французского языка
    ├── it/                     # Материалы для итальянского языка
    ├── ko/                     # Материалы для корейского языка
    ├── ru/                     # Материалы для русского языка
    └── uk/                     # Материалы для украинского языка
```

## Чек-лист добавления нового языка

Перед завершением процесса добавления нового языка убедитесь, что вы выполнили все шаги:

- [ ] Создан файл .arb с переводами для нового языка
- [ ] Создан класс локализации для нового языка
- [ ] **⚠️ Добавлена локаль в supportedLocales в main.dart (КРИТИЧЕСКИ ВАЖНО)**
- [ ] Обновлен app_localizations.dart (импорт, список локалей, функции)
- [ ] Добавлен язык в language_screen.dart (список языков и функция getLanguageName)
- [ ] Добавлено название языка во все .arb файлы
- [ ] **⚠️ Добавлены переводы промптов AI в assets/config/prompts.json (КРИТИЧЕСКИ ВАЖНО)**
- [ ] Созданы и переведены файлы в store_listings/shared/{lang}/
- [ ] Выполнена регенерация файлов локализации (flutter gen-l10n)
- [ ] Проведено тестирование нового языка в приложении
- [ ] Проверена работа AI-ассистента на новом языке

## Заключение

Следуя этой инструкции, вы сможете добавить любой новый язык в приложение. Процесс включает создание файла переводов, генерацию класса локализации, обновление соответствующих файлов конфигурации и создание материалов для store listings.

**Особое внимание:** Уделите особое внимание шагу 3 (обновление supportedLocales в main.dart), шагу 7 (локализация промптов AI) и шагу 8 (создание материалов для store listings), так как пропуск этих шагов является наиболее частой причиной того, что новый язык работает некорректно.

После добавления нового языка обязательно проведите тщательное тестирование всех экранов приложения, чтобы убедиться в корректности переводов и отсутствия пропущенных строк.

### ⚠️ Дополнительная проверка для AI-ассистента

После добавления нового языка обязательно проверьте работу AI-ассистента:

1. Откройте чат с AI-ассистентом
2. Убедитесь, что приветственное сообщение отображается на новом языке
3. Задайте вопрос и убедитесь, что AI отвечает на новом языке
4. Проверьте сообщения об ошибках (можно временно отключить интернет)
5. Протестируйте контекстные сообщения после сканирования продукта

Если AI-ассистент продолжает отвечать на английском языке, проверьте правильность добавления промптов в файл [`assets/config/prompts.json`](assets/config/prompts.json).

### ⚠️ Дополнительная проверка для Store Listings

После добавления нового языка обязательно проверьте материалы для store listings:

1. Откройте все файлы в [`store_listings/shared/{lang}/`](store_listings/shared/)
2. Убедитесь, что все файлы переведены
3. Проверьте соблюдение ограничений по количеству символов
4. Убедитесь, что ключевые слова разделены запятыми без пробелов
5. Проверьте, что название приложения соответствует ограничениям для обоих сторов

Это обеспечит корректное отображение приложения в Google Play Store и Apple App Store для нового языка.