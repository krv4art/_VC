# План оптимизации системы голосования

## Проблема
Система голосования в текущей реализации генерирует слишком много запросов к БД:
- **На загрузку главной**: 3 отдельных запроса (можно оптимизировать до 1-2)
- **За одно голосование**: 5-6 запросов (можно снизить до 1-2)
- **При отмене голоса**: 5-6 запросов (можно снизить до 1-2)

## Цель
Снизить количество запросов к БД на 60-80% без потери функциональности.

---

## Решение

### Уровень 1: Postgres функция (самое важное)

**Проблема:** При голосовании на линии 94-127 в `poll_supabase_service.dart` происходит:
1. Query: Проверить существующие голоса (`getUserVotes()`)
2. Query: Получить текущее значение vote_count
3. Mutation: Вставить голос в таблицу
4. Mutation: Обновить vote_count (+1)
= **4 операции за голос**

**Решение:** Создать Postgres функцию `vote_for_option(device_id, option_id)`, которая:
- Проверит лимит голосов в одной транзакции
- Проверит дублирование в одной транзакции
- Вставит голос и инкрементит vote_count атомарно
- Вернёт результат (success/error)

**Файл для создания:**
```
migrations/add_vote_function.sql
```

**SQL код:**
```sql
CREATE OR REPLACE FUNCTION vote_for_option(
  p_device_id UUID,
  p_option_id UUID
) RETURNS jsonb AS $$
DECLARE
  v_vote_count INT;
  v_existing_votes INT;
  v_already_voted BOOLEAN;
BEGIN
  -- Проверяем количество существующих голосов
  SELECT COUNT(*) INTO v_existing_votes
  FROM poll_votes
  WHERE device_id = p_device_id;

  IF v_existing_votes >= 3 THEN
    RETURN jsonb_build_object('success', false, 'error', 'max_votes_reached');
  END IF;

  -- Проверяем, не проголосовал ли уже за эту опцию
  SELECT EXISTS(
    SELECT 1 FROM poll_votes
    WHERE device_id = p_device_id AND option_id = p_option_id
  ) INTO v_already_voted;

  IF v_already_voted THEN
    RETURN jsonb_build_object('success', false, 'error', 'already_voted');
  END IF;

  -- Вставляем голос
  INSERT INTO poll_votes (device_id, option_id, created_at, updated_at)
  VALUES (p_device_id, p_option_id, NOW(), NOW());

  -- Инкрементим vote_count атомарно
  UPDATE poll_options
  SET vote_count = vote_count + 1, updated_at = NOW()
  WHERE id = p_option_id;

  RETURN jsonb_build_object('success', true);
END;
$$ LANGUAGE plpgsql;
```

**Аналогично для отмены голоса:** `unvote_for_option(device_id, option_id)`

**Профит:**
- Было: 4 операции
- Станет: 1 операция (RPC call)
- Экономия: **75% запросов за голос**

---

### Уровень 2: Убрать редундантный запрос в `getRemainingVotes()`

**Текущая проблема:** [poll_service.dart:287-289](lib/services/poll_service.dart#L287)
```dart
Future<int> getRemainingVotes() async {
  final votes = await getUserVotes();  // ← Это вызывает Query снова!
  return _maxVotes - votes.length;
}
```

**Решение:** В `poll_widget.dart` строка 62, вместо:
```dart
final remaining = await _pollService.getRemainingVotes();
```

Использовать:
```dart
final remaining = _maxVotes - votes.length;  // Вычислить локально
```

**Файлы для изменения:**
- [poll_widget.dart:53-77](lib/widgets/poll_widget.dart#L53) - функция `_loadPollData()`
- [poll_service.dart:287-290](lib/services/poll_service.dart#L287) - удалить функцию `getRemainingVotes()`

**Профит:**
- Было: +1 лишний запрос
- Станет: 0 лишних запросов
- Экономия: **1 запрос на загрузку**

---

### Уровень 3: Умная инвалидация кэша

**Текущая проблема:** [poll_service.dart:235-238](lib/services/poll_service.dart#L235)
```dart
if (success) {
  _cachedOptions = [];      // ← Чистит всё
  _cachedVotes = [];        // ← Чистит всё
}
```

При успешном голосе оба кэша полностью очищаются, что заставляет переодгружать всё при следующем обращении.

**Решение 1 (быстро):** Очищать только votes, опции не меняются
```dart
if (success) {
  _cachedVotes = [];  // ← Только голоса
  // _cachedOptions остаётся в кэше
}
```

**Решение 2 (оптимально):** Обновлять кэш локально после операции
```dart
Future<bool> vote(String optionId) async {
  try {
    final deviceId = await getDeviceId();
    final newVote = await _supabaseService.vote(deviceId, optionId);

    if (newVote != null) {
      // Добавляем новый голос в кэш
      _cachedVotes.add(newVote);

      // Обновляем vote_count в опциях
      final optionIndex = _cachedOptions.indexWhere((o) => o.id == optionId);
      if (optionIndex != -1) {
        final updatedOption = _cachedOptions[optionIndex];
        _cachedOptions[optionIndex] = PollOption(
          id: updatedOption.id,
          text: updatedOption.text,
          voteCount: updatedOption.voteCount + 1,  // ← +1
          createdAt: updatedOption.createdAt,
          isUserCreated: updatedOption.isUserCreated,
        );
      }
      return true;
    }
    return false;
  } catch (e) {
    // При ошибке очищаем и перезагружаем
    _cachedOptions = [];
    _cachedVotes = [];
    return false;
  }
}
```

**Профит:**
- Было: Full reload (3 запроса)
- Станет: No reload needed
- Экономия: **3 запроса за голос**

---

### Уровень 4: Консолидированный запрос на загрузку

**Текущее состояние:** 3 параллельных запроса в `poll_widget.dart:60-62`
```dart
final options = await _pollService.getOptions(_currentLanguage);
final votes = await _pollService.getUserVotes();
final remaining = await _pollService.getRemainingVotes();
```

**Идеальное решение:** Создать single endpoint в Supabase:

```sql
-- Функция получить всю информацию пользователя разом
CREATE OR REPLACE FUNCTION get_poll_data(p_device_id UUID, p_language_code TEXT)
RETURNS jsonb AS $$
BEGIN
  RETURN jsonb_build_object(
    'options', (
      SELECT jsonb_agg(jsonb_build_object(
        'id', po.id,
        'text', pot.text,
        'vote_count', po.vote_count,
        'created_at', po.created_at,
        'is_user_created', po.is_user_created
      ))
      FROM poll_options po
      INNER JOIN poll_option_translations pot
        ON po.id = pot.option_id
      WHERE pot.language_code = p_language_code
      ORDER BY po.created_at DESC
    ),
    'votes', (
      SELECT jsonb_agg(jsonb_build_object(
        'id', pv.id,
        'option_id', pv.option_id,
        'device_id', pv.device_id,
        'voted_at', pv.created_at
      ))
      FROM poll_votes pv
      WHERE pv.device_id = p_device_id
    )
  );
END;
$$ LANGUAGE plpgsql;
```

Вызов из Flutter:
```dart
Future<PollData> getPollData(String deviceId, String languageCode) async {
  final response = await _client
      .rpc('get_poll_data', params: {
        'p_device_id': deviceId,
        'p_language_code': languageCode,
      })
      .single();

  return PollData.fromJson(response);
}
```

**Профит:**
- Было: 3 запроса
- Станет: 1 запрос
- Экономия: **67% запросов на загрузку**

---

## Реализация по приоритетам

### ✅ **Приоритет 1: CRITICAL (делать первым)**
1. Создать Postgres функции для голосования
2. Обновить `poll_supabase_service.dart` использовать RPC вместо отдельных операций
3. Убрать редундантный `getRemainingVotes()`

**Ожидаемый результат:**
- Голос: 4 запроса → 1 запрос (-75%)
- Загрузка: 3 запроса → 2 запроса (-33%)

**Время реализации:** 2-3 часа

---

### ⚠️ **Приоритет 2: HIGH (делать вторым)**
4. Реализовать умное обновление кэша (Решение 2)
5. Обновить `poll_service.dart` и `poll_widget.dart`

**Ожидаемый результат:**
- После голоса: Full reload → No reload (-100%)
- Ощущаемая скорость значительно улучшится

**Время реализации:** 1-2 часа

---

### 📋 **Приоритет 3: NICE-TO-HAVE (делать последним)**
6. Создать consolidated endpoint `get_poll_data()`
7. Рефакторить `_loadPollData()` в `poll_widget.dart`

**Ожидаемый результат:**
- Загрузка: 2 запроса → 1 запрос (-50%)

**Время реализации:** 1-2 часа

---

## Тестирование

### Что проверить:
1. ✅ Голосование работает (лимит не превышен)
2. ✅ Отмена голоса работает
3. ✅ Лимит 3 голоса соблюдается
4. ✅ Нельзя проголосовать дважды за одно
5. ✅ Счётчики vote_count обновляются корректно
6. ✅ Кэш обновляется корректно
7. ✅ Работает offline режим (SharedPreferences fallback)

### Как проверить в Firebase Console:
1. Открыть Supabase → Logs → API
2. Посчитать количество запросов при одном голосе
3. Сравнить с изначальными 5-6 запросами

---

## Файлы для изменения

| Файл | Изменения | Строки |
|------|-----------|--------|
| `lib/services/poll_supabase_service.dart` | Добавить `vote_rpc()`, `unvote_rpc()`, обновить `vote()` и `unvote()` | 89-179 |
| `lib/services/poll_service.dart` | Удалить `getRemainingVotes()`, обновить `vote()`, `unvote()` с кэшированием | 228-290 |
| `lib/widgets/poll_widget.dart` | Вычислять remaining локально, убрать `getRemainingVotes()` call | 60-62, 208-209 |
| `migrations/add_vote_function.sql` | **НОВЫЙ** - Postgres функции `vote_for_option()` и `unvote_for_option()` | - |

---

## Риски и смягчение

| Риск | Смягчение |
|------|-----------|
| Функции Postgres не создадутся | Использовать Supabase UI для создания, протестировать в console |
| RPC вызовы будут медленнее | Они делают 1 операцию вместо 4, всё равно быстрее |
| Кэширование усложнит логику | Добавить unit-тесты для кэша, все ещё проще чем сейчас |
| Offline режим сломается | SharedPreferences fallback остаётся, его не трогаем |

---

## Результаты после внедрения

### Метрики

**До оптимизации:**
```
Загрузка главной:   3 запроса
Одно голосование:   5-6 запросов
Отмена голоса:      5-6 запросов
Всего в сессии:     13-15 запросов на типичную работу
```

**После оптимизации (Приоритет 1+2):**
```
Загрузка главной:   2 запроса (-33%)
Одно голосование:   1 запрос (-80%)
Отмена голоса:      1 запрос (-80%)
Всего в сессии:     4 запроса (-73%)
```

**После полной оптимизации (все приоритеты):**
```
Загрузка главной:   1 запрос (-67%)
Одно голосование:   1 запрос (-80%)
Отмена голоса:      1 запрос (-80%)
Всего в сессии:     3 запроса (-80%)
```

### Преимущества для пользователя:
- 🚀 Голосование отзывается мгновенно (нет ожидания reload)
- 📊 Счётчики обновляются без задержки
- 🌍 Меньше трафика и нагрузки на БД
- ⚡ UI более отзывчив (меньше setState'ов)
- 💾 Экономия трафика ~70%
