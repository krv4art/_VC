# Настройка уведомлений о негативных отзывах в Telegram

Этот гайд поможет вам настроить отправку негативных отзывов из приложения в ваш Telegram.

## Шаг 1: Создание Telegram бота

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/botfather)
2. Отправьте команду `/newbot`
3. Введите имя для вашего бота (например, "ACS Feedback Bot")
4. Введите username для бота (должен заканчиваться на "bot", например, "acs_feedback_bot")
5. BotFather пришлет вам токен бота. Он выглядит примерно так:
   ```
   123456789:ABCdefGHIjklMNOpqrsTUVwxyz
   ```
   **Сохраните этот токен!**

## Шаг 2: Получение Chat ID

### Вариант A: Личные сообщения (если хотите получать уведомления лично)

1. Найдите вашего бота в Telegram по username
2. Нажмите "Start" или отправьте боту любое сообщение
3. Откройте в браузере следующую ссылку (замените `<TOKEN>` на токен вашего бота):
   ```
   https://api.telegram.org/bot<TOKEN>/getUpdates
   ```
4. В ответе найдите поле `"chat":{"id":123456789}`
5. Это число `123456789` и есть ваш chat_id

### Вариант B: Группа/канал (если хотите получать уведомления в группу)

1. Создайте группу или канал в Telegram
2. Добавьте вашего бота в эту группу/канал как администратора
3. Отправьте любое сообщение в группу/канал
4. Откройте в браузере ту же ссылку:
   ```
   https://api.telegram.org/bot<TOKEN>/getUpdates
   ```
5. В ответе найдите `"chat":{"id":-100123456789}`
6. Chat ID группы/канала будет отрицательным числом (например, `-100123456789`)

## Шаг 3: Настройка приложения

1. Откройте файл `lib/config/telegram_config.dart`
2. Замените значения:

```dart
class TelegramConfig {
  /// Вставьте сюда токен вашего бота
  static const String botToken = '123456789:ABCdefGHIjklMNOpqrsTUVwxyz';

  /// Вставьте сюда ваш chat_id
  static const String chatId = '123456789';

  /// Установите true для включения отправки
  static const bool enabled = true;
}
```

## Шаг 4: Тестирование

Вы можете протестировать настройку, добавив тестовую функцию в ваше приложение:

```dart
// В любом месте вашего кода (например, в настройках)
await TelegramService().sendTestMessage();
```

Если настройка выполнена правильно, бот пришлет вам тестовое сообщение.

## Шаг 5: Отключение уведомлений (опционально)

Если вы хотите временно отключить отправку уведомлений (например, во время разработки):

```dart
class TelegramConfig {
  static const bool enabled = false; // Отключить отправку
}
```

## Что отправляется в Telegram?

Негативные отзывы отправляются в следующих случаях:

1. **Пользователь нажал "Нет"** на первом экране диалога оценки
2. **Пользователь поставил 1-4 звезды** (меньше максимальной оценки)
3. **Пользователь оставил текстовый отзыв** через форму обратной связи

Каждое сообщение включает:
- Оценку (если есть)
- Текст отзыва (если есть)
- Платформу (Android/iOS/Web)
- Шаг, на котором был оставлен отзыв
- Дату и время

## Формат сообщений

Сообщения приходят в красиво отформатированном виде с хештегами для фильтрации:

```
😞 Негативный отзыв

⭐ Оценка: 2/5 #rating2 #bad

💬 Отзыв:
Приложение работает медленно

ℹ️ Информация:
  • Platform: TargetPlatform.android
  • Step: Rating step

🕐 30.10.2025 14:35
```

### Хештеги для фильтрации

Каждое сообщение содержит хештеги для удобной фильтрации:

- `#rating0 #not_really` - пользователь нажал "Нет" на первом экране
- `#rating1 #very_bad` - оценка 1 звезда
- `#rating2 #bad` - оценка 2 звезды
- `#rating3 #neutral` - оценка 3 звезды
- `#rating4 #good` - оценка 4 звезды
- `#rating5 #excellent` - оценка 5 звезд (обычно не отправляются)

**Как использовать хештеги:**
- Нажмите на хештег (например, `#rating1`) чтобы увидеть все отзывы с этой оценкой
- Или используйте поиск в Telegram для поиска по хештегам

## Безопасность

**ВАЖНО:** Не коммитьте файл `telegram_config.dart` с реальными токенами в публичный репозиторий!

Добавьте в `.gitignore`:
```
lib/config/telegram_config.dart
```

## Troubleshooting

### Сообщения не приходят

1. Проверьте, что `enabled = true` в `telegram_config.dart`
2. Убедитесь, что токен и chat_id указаны правильно
3. Проверьте, что бот не заблокирован
4. Для групп: убедитесь, что бот добавлен как администратор

### Ошибка "Unauthorized"

- Проверьте правильность токена бота

### Ошибка "Chat not found"

- Проверьте правильность chat_id
- Для личных сообщений: убедитесь, что вы написали боту хотя бы одно сообщение
- Для групп: убедитесь, что бот добавлен в группу

## Дополнительные возможности

Вы можете расширить функциональность, редактируя файл `lib/services/telegram_service.dart`:

- Добавить отправку позитивных отзывов
- Изменить формат сообщений
- Добавить дополнительную информацию (версия приложения, device info и т.д.)
- Настроить фильтрацию (например, отправлять только отзывы с оценкой 1-2)
