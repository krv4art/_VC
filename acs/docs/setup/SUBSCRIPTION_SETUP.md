# Руководство по настройке подписок RevenueCat

## Содержание
1. [Что уже сделано](#что-уже-сделано-)
2. [Настройка RevenueCat Dashboard](#что-нужно-сделать-в-revenuecat-dashboard-)
3. [Тестирование подписок](#что-нужно-сделать-для-тестирования-)
4. [Использование подписок в коде](#как-использовать-подписки-в-коде-)
5. [Система ограничений для бесплатных пользователей](#система-ограничений-для-бесплатных-пользователей-) ⭐ **Новое**
6. [Важные замечания](#важные-замечания-)
7. [Дальнейшие улучшения](#дальнейшие-улучшения-)
8. [Troubleshooting](#troubleshooting-)

---

## Что уже сделано ✅

### 1. Минимальная интеграция библиотеки
- ✅ Добавлен `purchases_flutter: ^8.2.3` в pubspec.yaml
- ✅ Google Play Billing Library теперь присутствует в приложении
- ✅ Инициализация RevenueCat SDK в main.dart с API Key
- ✅ minSdk установлен в 24 для совместимости

### 2. Созданные файлы
- ✅ `lib/services/subscription_service.dart` - сервис для работы с RevenueCat
- ✅ `lib/providers/subscription_provider.dart` - provider для управления статусом
- ✅ `lib/utils/subscription_helper.dart` - хелперы для проверки премиум статуса
- ✅ `lib/screens/modern_paywall_screen.dart` - интегрирован с RevenueCat
- ✅ `lib/services/usage_tracking_service.dart` - отслеживание использования для бесплатных пользователей
- ✅ `lib/screens/usage_limits_test_screen.dart` - тестовый экран для проверки лимитов (только для разработки)

### 3. Интеграция в приложение
- ✅ SubscriptionProvider добавлен в MultiProvider в main.dart
- ✅ Автоматическая проверка статуса подписки при запуске
- ✅ UsageTrackingService инициализируется в main.dart
- ✅ Paywall экран полностью функционален
- ✅ Система ограничений для бесплатных пользователей реализована
- ✅ Конфигурация лимитов через `.env` файл

---

## Что нужно сделать в RevenueCat Dashboard 🔧

### Шаг 1: Создать Entitlement (Права доступа)
1. Откройте [RevenueCat Dashboard](https://app.revenuecat.com/)
2. Перейдите в **Entitlements** → **+ New**
3. Создайте entitlement с идентификатором: **`premium`**
4. Сохраните

### Шаг 2: Создать Offering (Предложение)
1. Перейдите в **Offerings** → **+ New**
2. Создайте offering с идентификатором: **`default`** (или любым другим)
3. Установите его как **Current Offering**

### Шаг 3: Привязать Products к Offering
1. В созданном Offering нажмите **+ Add Package**
2. Выберите ваши 3 подписки из Google Play Console
3. Для каждой подписки:
   - Привяжите **Entitlement**: `premium`
   - Можете добавить метаданные (описание, badge "Popular" и т.д.)

**Пример структуры:**
```
Offering: "default"
├─ Package: monthly_subscription
│  └─ Entitlement: premium
├─ Package: yearly_subscription
│  └─ Entitlement: premium
└─ Package: lifetime_subscription
   └─ Entitlement: premium
```

---

## Что нужно сделать для тестирования 🧪

### 1. Создать тестового пользователя в Google Play Console
1. Перейдите в Google Play Console → **Setup** → **License testing**
2. Добавьте свой email в список тестеров
3. Это позволит тестировать подписки без реальных платежей

### 2. Загрузить новый AAB в Google Play Console
```bash
flutter build appbundle --release
```

Загрузите файл `build/app/outputs/bundle/release/app-release.aab` в:
- **Testing → Closed testing** (или Internal testing)
- Google Play Console теперь распознает Billing Library ✅
- Раздел **Monetize → Subscriptions** будет разблокирован ✅

### 3. Протестировать подписки
1. Установите приложение из тестового трека
2. Откройте paywall экран (route: `/modern-paywall`)
3. Попробуйте приобрести тестовую подписку
4. Google Play покажет диалог с надписью "Test purchase - you won't be charged"

---

## Как использовать подписки в коде 💻

### Вариант 1: Проверка перед действием
```dart
import 'package:acs/utils/subscription_helper.dart';

void onPremiumFeatureTap() {
  if (!SubscriptionHelper.checkPremiumOrShowPaywall(context)) {
    return; // Paywall будет показан автоматически
  }

  // Выполняем премиум действие
  doSomethingPremium();
}
```

### Вариант 2: Условный рендеринг UI
```dart
import 'package:provider/provider.dart';
import 'package:acs/providers/subscription_provider.dart';

Widget build(BuildContext context) {
  final isPremium = context.watch<SubscriptionProvider>().isPremium;

  return ElevatedButton(
    onPressed: isPremium ? _doPremiumAction : _showPaywall,
    child: Text(isPremium ? 'Premium Feature' : 'Upgrade to Premium'),
  );
}
```

### Вариант 3: Premium Gate Widget
```dart
import 'package:acs/utils/subscription_helper.dart';

SubscriptionHelper.premiumGate(
  context: context,
  premiumChild: PremiumContentWidget(),
  freeChild: Text('Upgrade to access this feature'),
)
```

### Вариант 4: Прямая проверка
```dart
final subscriptionProvider = context.read<SubscriptionProvider>();
if (subscriptionProvider.isPremium) {
  // Показываем премиум контент
}
```

---

## Система ограничений для бесплатных пользователей 🔒

### Обзор
Приложение использует **UsageTrackingService** для отслеживания использования функций бесплатными пользователями. Счетчики хранятся локально в **SharedPreferences** и автоматически сбрасываются по расписанию.

### Реализованные ограничения

#### 1. Сканирование (5 сканов в неделю) 📸
- **Лимит:** 5 сканов на неделю
- **Сброс:** Автоматически через 7 дней с момента первого скана
- **Где реализовано:** `lib/screens/scanning_screen.dart`
- **Проверка:** Перед началом съемки и перед обработкой фото из галереи
- **Счетчик увеличивается:** После успешного сохранения результата анализа

**Поведение:**
- При достижении лимита показывается AlertDialog "Scan Limit Reached"
- Пользователю предлагается апгрейд до Premium
- Premium пользователи получают unlimited сканирование

#### 2. Сообщения в чате (5 ответов бота в день) 💬
- **Лимит:** 5 ответов от AI-бота в день
- **Сброс:** Каждый день в 00:00
- **Где реализовано:** `lib/screens/chat_ai_screen.dart`
- **Проверка:** Перед отправкой сообщения пользователя
- **Счетчик увеличивается:** После получения и сохранения ответа от бота

**Важно:** Считаются только **ответы бота**, а не сообщения пользователя. Это означает, что пользователь может отправить сообщение только если не исчерпан лимит на получение ответов.

**Поведение:**
- При достижении лимита показывается AlertDialog "Daily Message Limit Reached"
- Под полем ввода отображается индикатор: "X messages left today"
- При лимите = 0 показывается баннер с кнопкой "Upgrade"

#### 3. История сканирования (1 видимая позиция) 📜
- **Лимит:** Доступна только первая позиция в истории (index 0)
- **Где реализовано:** `lib/screens/scan_history_screen.dart`
- **Проверка:** При рендеринге каждой карточки в списке

**Поведение:**
- Первая карточка (самый свежий скан) отображается полностью
- Остальные карточки показываются с эффектом блюра (BackdropFilter)
- На заблокированных карточках отображается:
  - Иконка замка (48px)
  - Кнопка "Upgrade to View" (зеленая)
- При клике на заблокированную карточку - диалог с предложением апгрейда

#### 4. Результаты анализа (блокировка функций) 🔬
**Где реализовано:** `lib/screens/analysis_results_screen.dart`

**4.1. Подсказки к ингредиентам:**
- Иконка меняется с `info_outline` на `lock_outline`
- При клике - диалог "Premium Feature"
- Текст: "Detailed ingredient explanations are available with Premium subscription!"

**4.2. Кнопка "Discuss with AI":**
- Проверяется лимит сообщений (связано с ограничением #2)
- Если лимит исчерпан:
  - Текст кнопки: "Upgrade to Chat"
  - Иконка: замок вместо чата
  - Градиент меняется на серый
  - При клике - диалог с предложением апгрейда

### Конфигурация лимитов

Лимиты настраиваются через файл `assets/config/.env`:

```env
# Free user limitations
FREE_SCANS_PER_WEEK=5
FREE_MESSAGES_PER_DAY=5
FREE_VISIBLE_SCANS=1
```

Эти значения загружаются в `lib/config/app_config.dart` и используются в UsageTrackingService.

### Примеры использования в коде

#### Проверка лимита сканирования
```dart
import 'package:provider/provider.dart';
import '../providers/subscription_provider.dart';
import '../services/usage_tracking_service.dart';

Future<void> beforeScan(BuildContext context) async {
  final subscriptionProvider = context.read<SubscriptionProvider>();
  final usageService = UsageTrackingService();

  // Проверяем только для бесплатных пользователей
  if (!subscriptionProvider.isPremium) {
    final canScan = await usageService.canUserScan();

    if (!canScan) {
      // Показываем диалог с предложением апгрейда
      showUpgradeDialog(context);
      return;
    }
  }

  // Выполняем скан...
}

// После успешного скана
await usageService.incrementScansCount();
```

#### Проверка лимита сообщений
```dart
Future<void> beforeSendMessage(BuildContext context) async {
  final subscriptionProvider = context.read<SubscriptionProvider>();

  if (!subscriptionProvider.isPremium) {
    final canSend = await UsageTrackingService().canUserSendMessage();

    if (!canSend) {
      showUpgradeDialog(context);
      return;
    }
  }

  // Отправляем сообщение...
  // После получения ответа от бота:
  await UsageTrackingService().incrementMessagesCount();
}
```

#### Проверка доступности элемента истории
```dart
final usageService = UsageTrackingService();
final isPremium = context.watch<SubscriptionProvider>().isPremium;
final isAccessible = isPremium || usageService.isHistoryItemAccessible(index);

if (!isAccessible) {
  // Показываем заблокированную карточку с блюром
}
```

#### Получение оставшихся попыток
```dart
// Для сканов
final remaining = await UsageTrackingService().getRemainingScanCount();
print('Scans remaining: $remaining');

// Для сообщений
final remaining = await UsageTrackingService().getRemainingMessageCount();
print('Messages remaining: $remaining');
```

### Тестирование ограничений 🧪

#### Тестовый экран
Для удобства тестирования создан специальный экран: **`/usage-limits-test`**

**Как открыть:**
1. Откройте Profile screen
2. В разделе Settings найдите "Subscription Test" (помечено значком "TEST")
3. Или перейдите напрямую: `context.push('/usage-limits-test')`

**Функционал тестового экрана:**
- Показывает текущий статус подписки (ACTIVE/INACTIVE)
- Карточка "Weekly Scans":
  - Отображает: Used X/5, Remaining Y
  - Кнопка "Add Scan" - симулирует скан
  - Кнопка "Reset" - сбрасывает счетчик
- Карточка "Daily Messages":
  - Отображает: Used X/5, Remaining Y
  - Кнопка "Add Message" - симулирует сообщение
  - Кнопка "Reset" - сбрасывает счетчик
- Кнопка "Reset All Counters" - сбрасывает все счетчики сразу
- Кнопка перехода на Paywall

#### Сброс счетчиков программно
```dart
// Сбросить все счетчики
await UsageTrackingService().resetAllCounters();

// Сбросить только сканы
await UsageTrackingService().resetWeeklyScansCount();

// Сбросить только сообщения
await UsageTrackingService().resetDailyMessagesCount();
```

#### Сценарии тестирования

**Сценарий 1: Проверка лимита сканов**
1. Откройте тестовый экран
2. Нажмите "Add Scan" 5 раз
3. Попробуйте сделать реальный скан
4. Должен появиться диалог "Scan Limit Reached"

**Сценарий 2: Проверка лимита сообщений**
1. Откройте тестовый экран
2. Нажмите "Add Message" 5 раз
3. Откройте любой чат
4. Попробуйте отправить сообщение
5. Должен появиться диалог "Daily Message Limit Reached"

**Сценарий 3: Проверка истории сканов**
1. Создайте минимум 2 скана
2. Откройте History screen
3. Первая карточка должна быть видна
4. Вторая и последующие - с блюром и замком

**Сценарий 4: Проверка результатов анализа**
1. Откройте любой результат скана
2. Нажмите на иконку "?" у ингредиента
3. Для бесплатных - должен быть замок и диалог
4. Если лимит сообщений исчерпан - FAB показывает "Upgrade to Chat"

### Техническая реализация

#### Хранение данных
UsageTrackingService использует **SharedPreferences** для хранения:
- `weekly_scans_count` - количество сканов за неделю
- `weekly_scans_reset_date` - дата сброса счетчика сканов
- `daily_messages_count` - количество сообщений за день
- `daily_messages_reset_date` - дата сброса счетчика сообщений

#### Автоматический сброс
Счетчики проверяются и сбрасываются автоматически при каждом вызове методов:
- `getWeeklyScansCount()` - проверяет, прошло ли 7 дней
- `getDailyMessagesCount()` - проверяет, изменился ли день

#### Инициализация
```dart
// В main.dart
await UsageTrackingService().initialize();
```

Это гарантирует, что счетчики проверены и сброшены при запуске приложения.

### Файлы с реализацией ограничений

| Файл | Что реализовано |
|------|----------------|
| `lib/services/usage_tracking_service.dart` | Основной сервис отслеживания |
| `lib/screens/scanning_screen.dart` | Лимит на сканирование (5/неделю) |
| `lib/screens/chat_ai_screen.dart` | Лимит на сообщения (5/день) |
| `lib/screens/scan_history_screen.dart` | Блюр недоступных карточек истории |
| `lib/screens/analysis_results_screen.dart` | Блокировка подсказок и AI чата |
| `lib/screens/usage_limits_test_screen.dart` | Тестовый экран (только dev) |
| `lib/config/app_config.dart` | Конфигурация лимитов из .env |
| `assets/config/.env` | Значения лимитов |

---

## Важные замечания ⚠️

### 1. Entitlement ID должен совпадать
В `lib/services/subscription_service.dart` указан:
```dart
static const String premiumEntitlement = 'premium';
```

Убедитесь, что в RevenueCat Dashboard вы создали Entitlement с **точно таким же идентификатором**: `premium`

### 2. API Key уже настроен
API Key для Android уже добавлен в код:
```dart
goog_UKfcCZSMJPjqYxRVOMbiPXbkRoS
```

Для iOS нужно будет добавить отдельный ключ (если планируете iOS версию).

### 3. Подписки в Google Play Console
После загрузки AAB с Billing Library, создайте 3 подписки в:
**Google Play Console → Monetize → Subscriptions → Create subscription**

Примеры идентификаторов:
- `monthly_premium` - месячная подписка
- `yearly_premium` - годовая подписка
- `lifetime_premium` - пожизненная подписка

### 4. Проверка статуса подписки
SubscriptionProvider автоматически проверяет статус при запуске приложения.
Для ручной проверки:
```dart
final provider = context.read<SubscriptionProvider>();
await provider.checkStatus();
```

---

## Дальнейшие улучшения 🚀

### Уже реализовано:
- ✅ **Показ paywall при открытии премиум функций** - реализовано через диалоги
- ✅ **Ограничения для бесплатных пользователей** - 4 типа ограничений
- ✅ **Тестовый экран для проверки лимитов** - `/usage-limits-test`

### Опционально - можно добавить позже:
1. **Аналитика событий** - отслеживание покупок через Firebase/Amplitude
2. **Разные уровни подписок** - базовая, премиум, про
3. **Promo codes** - промокоды для бесплатного доступа
4. **Индикатор "Premium" в профиле пользователя** - визуальный badge
5. **Персонализированные предложения** - разные цены для разных пользователей

---

## Troubleshooting 🔍

### Проблема: "No offerings available"
**Решение:**
1. Проверьте, что в RevenueCat Dashboard созданы Offerings
2. Проверьте, что Products импортированы из Google Play
3. Проверьте, что хотя бы один Offering установлен как Current

### Проблема: "Purchase failed"
**Решение:**
1. Убедитесь, что вы тестируете на реальном устройстве (не эмулятор)
2. Проверьте, что ваш email добавлен в License testing в Google Play Console
3. Установите приложение из тестового трека (не через `flutter run`)

### Проблема: Подписка не активируется после покупки
**Решение:**
1. Проверьте, что Entitlement ID в коде совпадает с RevenueCat Dashboard
2. Вызовите `await provider.checkStatus()` после покупки
3. Проверьте логи RevenueCat (RevenueCat Dashboard → Customer → найдите пользователя)

---

## Контакты для помощи 📞

- [RevenueCat Documentation](https://docs.revenuecat.com/)
- [RevenueCat Flutter SDK](https://docs.revenuecat.com/docs/flutter)
- [Google Play Billing](https://developer.android.com/google/play/billing)
