# 🔐 Инструкция по настройке подписи приложения

## Проблема решена! ✅

Были внесены следующие изменения для устранения предупреждения "фальшивое приложение":

1. ✅ Package name изменён на уникальный: `com.ai.cosmetic.scanner.beauty.ingredients.analyzer`
2. ✅ Название приложения обновлено: `AI Cosmetic Scanner`
3. ✅ Настроена конфигурация для production подписи
4. ✅ Добавлена защита приватных ключей в `.gitignore`

---

## ⚠️ ВАЖНО: Создайте Production Keystore

Чтобы полностью устранить предупреждение, необходимо создать keystore для production:

### Шаг 1: Создайте keystore (один раз)

Откройте командную строку в папке проекта и выполните:

```bash
keytool -genkey -v -keystore acs-release-key.keystore -keyalg RSA -keysize 2048 -validity 10000 -alias acs-release
```

**При запросе данных укажите:**
- **Пароль keystore:** придумайте надёжный пароль и **сохраните его!**
- **Пароль ключа:** можете использовать тот же пароль
- **Имя и фамилия:** `AI Cosmetic Scanner`
- **Название организации:** ваша компания/имя
- **Город, область, страна:** ваши данные

**🚨 КРИТИЧЕСКИ ВАЖНО:**
- Сохраните файл `acs-release-key.keystore` в надёжное место
- Сохраните пароли в password manager
- Сделайте резервную копию keystore (без него невозможно обновлять приложение!)

---

### Шаг 2: Создайте файл key.properties

Скопируйте шаблон и заполните реальными данными:

```bash
copy android\key.properties.template android\key.properties
```

Откройте `android\key.properties` и замените:
- `YOUR_KEYSTORE_PASSWORD_HERE` → ваш реальный пароль keystore
- `YOUR_KEY_PASSWORD_HERE` → ваш реальный пароль ключа

Файл должен выглядеть так:

```properties
storePassword=ваш_пароль_keystore
keyPassword=ваш_пароль_ключа
keyAlias=acs-release
storeFile=../acs-release-key.keystore
```

**🔒 ВАЖНО:** Файл `key.properties` НЕ попадёт в Git (защищён через .gitignore)

---

### Шаг 3: Соберите production APK

```bash
# Очистить старые сборки
flutter clean

# Собрать release APK
flutter build apk --release
```

**Результат:** `build\app\outputs\flutter-apk\app-release.apk`

---

### Шаг 4: Проверьте подпись

```bash
keytool -printcert -jarfile build\app\outputs\flutter-apk\app-release.apk
```

**Должно показать:**
- Владелец: CN=AI Cosmetic Scanner (или ваши данные)
- **НЕ должно быть:** "Android Debug" или "CN=Android Debug"

---

### Шаг 5: Установите и протестируйте

1. Скопируйте APK на тестовое устройство
2. Установите приложение
3. ✅ **Предупреждения о "фальшивом приложении" не должно быть!**
4. Проверьте, что все функции работают

---

## 📱 Сборка для Google Play Store

Для публикации в Google Play Store используйте App Bundle:

```bash
flutter build appbundle --release
```

**Результат:** `build\app\outputs\bundle\release\app-release.aab`

---

## 🆘 Проблемы и решения

### Ошибка "keystore not found"
- Убедитесь, что `acs-release-key.keystore` находится в корне проекта (рядом с папкой `android`)
- Проверьте путь в `android/key.properties`

### Ошибка "incorrect password"
- Проверьте пароли в `android/key.properties`
- Убедитесь, что нет лишних пробелов

### APK не устанавливается
- Удалите старую версию приложения (package name изменился!)
- Включите "Установка из неизвестных источников" в настройках Android

### Всё ещё показывает предупреждение
- Убедитесь, что собрали APK ПОСЛЕ создания keystore
- Проверьте подпись командой `keytool -printcert`
- Если показывает "Android Debug", значит keystore не применился

---

## 📋 Что изменилось в проекте

### 1. android/app/build.gradle.kts
- Добавлена загрузка `key.properties`
- Изменён package: `com.ai.cosmetic.scanner.beauty.ingredients.analyzer`
- Настроен `signingConfigs` для production
- Release сборка теперь использует production ключ (если он есть)

### 2. android/app/src/main/AndroidManifest.xml
- Добавлен атрибут `package`
- Изменён `android:label` на "AI Cosmetic Scanner"

### 3. .gitignore
- Добавлена защита для `*.keystore`, `*.jks`, `key.properties`

### 4. Новые файлы
- `android/key.properties.template` - шаблон для конфигурации
- `SIGNING_SETUP.md` - эта инструкция

---

## ✅ Чеклист для релиза

Перед публикацией убедитесь:

- [ ] Keystore создан и сохранён в безопасном месте
- [ ] Пароли записаны в password manager
- [ ] Создана резервная копия keystore
- [ ] Файл `key.properties` создан с реальными паролями
- [ ] APK собран командой `flutter build apk --release`
- [ ] Подпись проверена (`keytool -printcert`) - НЕ debug
- [ ] Приложение протестировано на реальном устройстве
- [ ] Нет предупреждения безопасности при установке
- [ ] Все функции работают корректно

---

## 📞 Поддержка

При возникновении проблем обратитесь к документации:
- [Flutter Deployment Guide](https://docs.flutter.dev/deployment/android)
- [Android App Signing](https://developer.android.com/studio/publish/app-signing)

---

**Создано:** 2025-10-16
**Версия приложения:** 1.0.0+1
**Package:** com.ai.cosmetic.scanner.beauty.ingredients.analyzer
