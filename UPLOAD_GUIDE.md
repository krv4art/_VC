# Google Play Console Uploader - Полное руководство

Эта система автоматизирует загрузку описаний приложений на разные языки в Google Play Console.

## 📂 Структура файлов

```
acs/store_listings/shared/
├── en/
│   ├── name.txt                             # Название приложения (50 символов макс)
│   ├── short_description_google_play.txt    # Краткое описание (80 символов)
│   ├── description.txt                       # Полное описание (4000 символов)
│   ├── keywords.txt                         # Ключевые слова (100 символов)
│   └── promo_text_google_play.txt          # Промо-текст (80 символов)
├── ru/
├── el/
├── ja/
└── [другие языки...]
```

## 🚀 Способы загрузки

### Способ 1: Менеджер команд (Рекомендуется для подготовки)

Используйте `upload-manager.js` для подготовки и проверки данных перед загрузкой.

#### Установка зависимостей
```bash
npm init -y
```

#### Просмотр статистики
```bash
node upload-manager.js --stats
```

Выведет количество языков, полей и символов.

#### Экспортирование данных
```bash
node upload-manager.js --export
```

Создаст файлы:
- `upload-data.json` - JSON со всеми данными
- `upload-data.csv` - CSV таблица для просмотра
- `browser-upload-script.js` - готовый скрипт для браузера

#### Предпросмотр для конкретного языка
```bash
node upload-manager.js --preview en
node upload-manager.js --preview ru
```

### Способ 2: Скрипт для браузера (Способ загрузки)

Самый надежный способ для реальной загрузки в Google Play Console.

#### Шаг 1: Подготовьте скрипт
```bash
node upload-manager.js --export
```

#### Шаг 2: Откройте Google Play Console
1. Перейдите на https://play.google.com/console
2. Выберите приложение
3. Перейдите в **Store listing** или **Listing** (в зависимости от интерфейса)

#### Шаг 3: Откройте консоль браузера
- Нажмите **F12** или **Right-click → Inspect**
- Перейдите на вкладку **Console**

#### Шаг 4: Запустите скрипт
1. Откройте файл `browser-upload-script.js`
2. Скопируйте весь содержимое
3. Вставьте в консоль браузера
4. Нажмите Enter

#### Шаг 5: Загрузите язык
```javascript
// Для английского:
await uploadLanguage('en');

// Для русского:
await uploadLanguage('ru');

// Для греческого:
await uploadLanguage('el');

// И т.д. для остальных языков
```

#### Шаг 6: Сохраните
- Найдите кнопку **Save** (Сохранить)
- Нажмите её
- Google Play Console подтвердит сохранение

### Способ 3: Скрипт для браузера (Встроенный вариант)

Если вы не хотите экспортировать, можно использовать `browser-console-uploader.js` напрямую.

1. Откройте `browser-console-uploader.js`
2. Отредактируйте объект `uploadData`:
```javascript
const uploadData = {
  'App name': 'AI Cosmetic Scanner',
  'Short description': 'ваше краткое описание',
  'Full description': 'ваше полное описание',
  'Keywords': 'ключевые слова',
};
```
3. Скопируйте код в консоль браузера
4. Выполните: `startUpload()`

### Способ 4: Автоматизация через Puppeteer (Для опытных пользователей)

Используйте `google-play-uploader.js` для полной автоматизации с браузером.

#### Требования
```bash
npm install puppeteer
```

#### Запуск
```bash
node google-play-uploader.js
```

#### Что он делает
1. Запускает браузер
2. Открывает Google Play Console
3. Ждет вашего входа в систему
4. Заполняет поля для каждого языка
5. Сохраняет изменения

## 📋 Таблица полей Google Play Console

| Поле | Файл | Макс символов | Google Play | App Store |
|------|------|---------------|------------|-----------|
| Название | `name.txt` | 50 | ✓ | ✓ |
| Краткое описание | `short_description_google_play.txt` | 80 | ✓ | ✗ |
| Полное описание | `description.txt` | 4000 | ✓ | ✓ |
| Ключевые слова | `keywords.txt` | 100 | ✓ | ✗ |
| Промо-текст | `promo_text_google_play.txt` | 80 | ✓ | ✗ |

## ⚠️ Важные замечания

### Проверка длины текстов
- Скрипт автоматически проверяет длину каждого поля
- Если текст превышает лимит, вы получите предупреждение
- Слишком длинный текст будет обрезан Google Play Console

### Первый запуск
1. Сначала используйте `upload-manager.js --export` для проверки
2. Проверьте файлы в `upload-data.json` и `upload-data.csv`
3. При необходимости отредактируйте файлы `.txt`
4. Затем используйте скрипт для браузера

### Если скрипт не находит поля
Селекторы Google Play Console могут меняться. В таком случае:
1. Откройте консоль браузера (F12)
2. Вручную найдите селекторы для полей
3. Обновите переменные в скрипте

### Попробуйте найти поле вручную
```javascript
// В консоли браузера:
document.querySelectorAll('input, textarea')
// Найдите нужное поле и скопируйте его селектор
```

## 🔄 Рабочий процесс

### Для первой загрузки всех языков:

```bash
# 1. Подготовка
node upload-manager.js --export

# 2. Проверка результатов
node upload-manager.js --stats

# 3. Откройте Google Play Console и вкладку Console в браузере

# 4. Загрузите скрипт из browser-upload-script.js

# 5. В консоли браузера:
await uploadLanguage('en');
await uploadLanguage('ru');
await uploadLanguage('el');
// ... и т.д.

# 6. Нажмите Save в Google Play Console
```

### Для обновления одного языка:

```bash
# 1. Отредактируйте нужные файлы в acs/store_listings/shared/[lang]/

# 2. Используйте upload-manager.js для проверки
node upload-manager.js --preview [lang]

# 3. Загрузите только этот язык через браузер
await uploadLanguage('[lang]');
```

## 💡 Примеры использования

### Пример 1: Загрузка конкретного языка с проверкой
```bash
# Проверка русского языка
node upload-manager.js --preview ru

# Если всё ОК, переходим в браузер и выполняем:
await uploadLanguage('ru');
```

### Пример 2: Экспорт для редактирования в Excel/Sheets
```bash
node upload-manager.js --export

# Откройте upload-data.csv в Excel или Google Sheets
# Отредактируйте там
# Скопируйте обновленные значения обратно в .txt файлы
```

### Пример 3: Проверка перед первой загрузкой
```bash
# Посмотрите все данные
node upload-manager.js --all

# Проверьте статистику
node upload-manager.js --stats

# Если всё ОК, начните загрузку
```

## 🐛 Решение проблем

### Скрипт не находит папку `store_listings`
Убедитесь, что файлы находятся в:
```
e:\_VC\acs\store_listings\shared\[lang]\
```

### Google Play Console не сохраняет изменения
1. Проверьте, есть ли кнопка **Save**
2. Убедитесь, что у вас есть права на редактирование
3. Попробуйте сохранить вручную

### Скрипт пропускает некоторые поля
1. Селекторы могут быть другими в новой версии Google Play
2. Проверьте с помощью F12 → Elements
3. Обновите селекторы в скрипте

### Текст содержит символы, которые неправильно отображаются
Убедитесь, что файлы сохранены с кодировкой **UTF-8**:
- VS Code: выберите **UTF-8** в правом нижнем углу
- Блокнот: **Кодировка → UTF-8** при сохранении

## 📞 Если что-то не работает

1. Проверьте консоль браузера (F12) на наличие ошибок
2. Убедитесь, что вы вошли в Google Play Console
3. Проверьте, что язык существует в Google Play Console для вашего приложения
4. Попробуйте сохранить одно поле вручную, потом запустите скрипт

## 📝 Структура автоматически созданных файлов

После `node upload-manager.js --export` будут созданы:

### upload-data.json
```json
{
  "en": {
    "name": "AI Cosmetic Scanner",
    "shortDescription": "...",
    "description": "...",
    ...
  },
  "ru": { ... },
  ...
}
```

### upload-data.csv
Удобно для просмотра в Excel:
```
Language,name,shortDescription,description,keywords,promoText
en,"AI Cosmetic Scanner","...",
ru,"AI ...",
```

### browser-upload-script.js
Готовый скрипт с функциями:
- `uploadLanguage(lang)` - загрузить конкретный язык
- `typeText(element, text)` - вспомогательная функция для ввода
- `findFieldByLabel(label)` - поиск полей по названию

---

**Версия:** 1.0
**Последнее обновление:** 2025-11-10
